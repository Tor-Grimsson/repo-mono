# lobby

Intake queue for **kol-website** — content and UI issues.

One `.md` per issue at this level, screenshots in `_assets/`. File one with
`clip-drop.sh --kol-website NAME` (clipboard image + a timestamped embed,
appending on repeat). Read the queue with `/lobby`.

Not documentation — a work queue. It deliberately sits outside `docs/`.
