# lobby — kol-website

Intake portal for **kol-website**: content and UI issues dropped in from any repo.
Not documentation, not a tracker — a work queue, deliberately outside `docs/`.

**It is empty between drops.** A file here means work not yet done. Nothing parks
here, nothing waits here for the agent to approve it, nothing is held open because
it has not been implemented yet.

| | |
|---|---|
| File one | `clip-drop.sh --kol-website NAME` · `/lobby-web` (from any repo) |
| Read it | `/lobby-list` · `bin/lobby` · `prefix Ctrl+K` |
| The spec | `~/.dotfiles/docs/operations/systems/lobby/` |

## States

| | State | Means | Lives in |
|---|---|---|---|
| 🔵 | `filed` | Captured, unread | `inbox/` |
| 🟡 | `read` | Understood — the row restates it | `inbox/` |
| 🟠 | `addressed` | A change shipped that is meant to close it | `inbox/` |
| 🟢 | `closed` | The thing it asked for exists and is verified | `done/`, then out |
| ⚪ | `parked` | Deliberately not-now, reason recorded | `archive/` |

**Closing is purpose-served.** A ticket closes when the thing it asked for exists
and is verified. It is not held for sign-off. If the work was wrong, refile it.

**A decision is never a lobby row.** If what remains is the user choosing rather
than the agent building, the ticket closes and the choice goes to
`.kol/llm-context/AGENT-CONTEXT.md` § *Awaiting user rulings*.

**Closed entries leave.** They move to `.kol/llm-context/lobby-archive/` — agent
state belongs in `.kol/`, not in a live portal.

## Queue

| | Entry | Filed | Ask |
|---|---|---|---|

## Closed

| | Entry | Closed | Resolution |
|---|---|---|---|
| 🟢 | brand-redeploy-frees-media-hostname | 2026-08-26 | Deploy landed 2026-08-25 (`43eeb9e`); verified 2026-08-26: live bundle carries no `media.kolkrabbi.io`, `/library` loads 7/7 images from `r2.`. Receipt returned to kol-r2b2 so it can detach `media.` from the R2 bucket; entry graduated to `.kol/llm-context/lobby-archive/` |
| 🟢 | ArticleCardSizeSpec | 2026-08-15 | All five DoD items same-day (user rulings): presets cut to three (`readmore` dropped — a context, not a size), geometry table written (120×120 confirmed, clamps 2/3/2, breakpoint swaps consumer-side), scope ruled WIDE, name ruled `ListingCard` (aliases until next major). Spec filed to kol-ds-ui as `ListingCardSpec`; entry graduated to `.kol/llm-context/lobby-archive/` |
| 🟢 | [TiltFamilyForks](../.kol/llm-context/lobby-archive/TiltFamilyForks.md) | 2026-08-27 | **Executed same session.** kol-component `^0.110.0`; `HomeFoundry` + `WorkDetail` on the DS `TiltCard` (WorkDetail's was a dead import), `HomeHighlights`' seven tiles on `TiltBento` with `onNavigate` · `buttonLabel` · `titleClassName` passed explicitly so the render is unchanged; three forks retired to `_tmp/2026-08-27-tilt-forks/` (`useIsTouchDevice` stays — `HomeHero` imports it). Surfaced `TiltBentoVoiceAndDefaults`, filed back. Eyeball owed: the CSS-tilt → spring feel |

## Archived

_(none yet — ⚪ parked and ⚫ retired entries land in `archive/`)_

## Filed elsewhere

| | Receipt | Destination | Last known | Remainder here |
|---|---|---|---|---|
| 🟢 | [CardTagsNoVisibleFill](outbox/CardTagsNoVisibleFill.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — **component 0.158.0**: `tagVariant` seam, default `primary`; derived from the box, so the filled `/work` row keeps `tertiary` | ✅ executed 2026-09-01 — component ^0.158.0; 15/15 chips `kol-tag--primary` on `/`, 16% ink wash vs page. `/stack`'s grid passes no tags — NOT a ruling, an 08-27 agent comment matching the old live site; the user never decided it (corrected 2026-09-01), still open |
| 🟢 | [NewsletterFormGapOffLadder](outbox/NewsletterFormGapOffLadder.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — **component 0.158.0**: form on `gap-2 sm:gap-4` | ✅ executed 2026-09-01 — box to box on `/` at 390: input→Subscribe 8, Explore→View Docs 8 |
| 🟢 | [ProseTitleFixedSize](outbox/ProseTitleFixedSize.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — **theme 0.125.0**: `min(var(--kol-text-display-02), 56px)`, lh 1.07 — rides display-02, caps at 56, 44 on a phone | ✅ executed 2026-09-01 — theme 0.125.0; token verified in installed CSS, `/stack/:slug` fold is eyes-on-live (Sanity CORS) |
| 🟢 | [SectionNewsletterSubmitVariant](outbox/SectionNewsletterSubmitVariant.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — **component 0.157.0**: `submitVariant` prop, default `primary` | ✅ executed 2026-09-01 — component ^0.157.0; `HomeSignup.jsx` passes `secondary`; measured on `/` locally |
| 🟢 | [ProfileCard](outbox/ProfileCard.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — **component 0.154.0**: shelf sizes to content (`grid-template-rows 0fr → 1fr`, no guessed numbers), scale and width separated, and the disclosure ruled — a button with `aria-expanded`, `IconFrame secondary radius="full"` over media, bare `nav` on a surface; `ToggleSwitch` stays a form control | ✅ executed 2026-09-01 — component ^0.154.0; local file retired to `_tmp/2026-09-01-profilecard-ds-adoption/`, zero importers; verified at 390×700 with the shelf open: 204px, rack of five unclipped |
| 🟢 | [WorkshopSearchCloseUndoneBySetText](outbox/WorkshopSearchCloseUndoneBySetText.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — workshop 0.26.0: one closeSearch helper, provided path is closeTagMode() alone; onSelect's destination branch had the same pair, same fix | ✅ executed 2026-09-01 — workshop ^0.26.0; scrim tap closes under touch emulation, with and without a typed query |
| 🟢 | [OverlaySearchFieldZoomsIOS](outbox/OverlaySearchFieldZoomsIOS.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — theme 0.117.0 adds `.kol-control--bare` to the coarse 16px floor; component stamps it on SearchInput's bare plan | ✅ executed 2026-09-01 — theme 0.117.0 + component ^0.150.1; field measures 16px under coarse emulation; real-iPhone tap is the user's |
| 🟢 | [SectionSplitVisualHeightRemainder](outbox/SectionSplitVisualHeightRemainder.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — the bounded-frame height applies min-[901px] only; stacked = w-full + ratio, media never clips | ✅ executed 2026-09-01 — re-checked `/studio` at 390×700: card 350×350, unclipped |
| 🟢 | [ContentRowShowcaseImageDrivenHeight](outbox/ContentRowShowcaseImageDrivenHeight.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — showcase heightSm: 168, a FIXED rung below md; tags single-row below md by design | ✅ executed 2026-09-01 — bumped; `/work` still passes no height; look verifies on production (Sanity CORS blocks local) |
| 🟢 | [SectionNewsletterMobileFoot](outbox/SectionNewsletterMobileFoot.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — py-16 md:py-24; foot 96 → 64 at 390, symmetric, desktop unmoved | ✅ executed 2026-09-01 — bumped; nothing local to remove |
| 🟢 | [ContentTextTagsSlotRendersRawArray](outbox/ContentTextTagsSlotRendersRawArray.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — string tags render as tertiary sm Tag chips; any other array slot gets one span per item | ✅ executed 2026-09-01 — bumped; chips verify on production (Sanity CORS blocks local) |
| 🟢 | [ContentFiltersMobileGaps](outbox/ContentFiltersMobileGaps.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — title seam gap-3 md:gap-6 + pr-2 md:pr-4; facets gap-8 md:gap-16 | ✅ executed 2026-09-01 — bumped; `/work` and `/stack` still pass nothing |
| 🟢 | [OverlayScrimTapDismiss](outbox/OverlayScrimTapDismiss.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — the scrim is a `<button>`; same fix in ShellDrawer's backdrop | ⚠ re-check FAILED 2026-09-01 — the click fires but the close is undone by setText's force-open; successor `WorkshopSearchCloseUndoneBySetText` filed, closed same day — workshop 0.26.0, tap-close verified |
| 🟢 | [OverlayScrimBlur](outbox/OverlayScrimBlur.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-09-01 — blur dropped from `.kol-overlay-scrim`, the 60% tint stays, all three call sites | ✅ executed 2026-09-01 — theme 0.117.0; measured `backdrop-filter: none` |
| 🟢 | [TypefaceCardRestSkinFix](outbox/TypefaceCardRestSkinFix.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-30 — component 0.143.0: card takes the ROW's `transparent` border + `surface-secondary` fill | **none** — ✅ executed: bumped, card verified against the row's box |
| 🟢 | [TypefaceRowHoverBg](outbox/TypefaceRowHoverBg.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-30 — component 0.141.0: the `hover` bg key deleted, `frameHover` kept | **none** — ✅ executed: bumped, verified in the package |
| 🟢 | [TypefaceCardRestSkin](outbox/TypefaceCardRestSkin.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-30 — component 0.140.0: border fg-08→fg-04, bg surface-primary→null, nothing else moved | **none** — ✅ executed: bumped |
| 🟢 | [TypefaceRowSkinCorrection](outbox/TypefaceRowSkinCorrection.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-30 — component 0.139.0: `showcaseCanvas` keeps its own pad 24 / rung 160, takes only bg + frame | **none** — ✅ executed: bumped, the local stopgap removed |
| 🟢 | [TypefaceRowSkin](outbox/TypefaceRowSkin.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-30 — component 0.138.0: `showcaseCanvas` derives from `showcase` (`{...BOX_SHOWCASE, thumb: 0, column: true}`), one object, cannot drift | **none** — ✅ executed 2026-08-30: bumped |
| 🟢 | [PageGutterOwnership](outbox/PageGutterOwnership.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🔵 `filed` · 2026-08-30 | 📌 bump; every page container onto the ruled owner, delete per-page gutters, adopt on the four unruled routes |
| 🟢 | [RowRungAndFillThumb](outbox/RowRungAndFillThumb.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🔵 `filed` · 2026-08-30 | 📌 bump; replace `.kol-row.print-row`'s two `!important` lines with the prop(s) |
| 🟢 | [RowThumbRatioDead](outbox/RowThumbRatioDead.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-30 — theme 0.99.0 · component 0.135.0: the row publishes `--kol-row-thumb-ratio`; the rung is the thumb's HEIGHT so a non-square ratio widens it | **none** — ✅ executed 2026-08-30: prop restored, thumb measured 96.2 × 136 (0.707) |
| 🟢 | [RowVariantNamesAndSpecs](outbox/RowVariantNamesAndSpecs.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🔵 `filed` · 2026-08-29 | 📌 bump; move `/prints` + `/work` onto the shape names and pass `specs` (needs `material` / `edition` added to the 24 records first) |
| 🟢 | [MotionSheetLocalisation](outbox/MotionSheetLocalisation.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-28 — theme 0.89.0 · component 0.128.0: `kol-animation.css` minted; `.kol-reveal*` + `.kol-animated-word`, bare names aliased | **none** — ✅ executed 2026-08-28: bumped, `animations.css` 127 → 73 lines, the `AnimatedTitle` fork retired to `_tmp/` and its call site on the DS component |
| 🟢 | [PageHeaderTrailingSlot](outbox/PageHeaderTrailingSlot.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-28 — kol-shell 0.15.0: `PageHeader actions` on the lede's baseline · `subtitleMaxWidth` · `--kol-page-header-mb` | **none** — ✅ executed 2026-08-28: the lede is back in `PageHeader`, cluster passed as `actions`, the bare `<p>` + `!important` margin + max-width rule gone — `controls-tone.css` retired to `_tmp/` |
| 🟢 | [ViewToggleWellGap](outbox/ViewToggleWellGap.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-28 — component 0.121.1: the icon well is inset, so `gap-N` measures N on both sides | **none** — ✅ executed 2026-08-28: bumped, the `-ml-1` deleted |
| 🟢 | [ControlToneSunken](outbox/ControlToneSunken.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-28 — component 0.120.0 · theme 0.82.0 · framework 0.35.0: `tone="sunken"` on the whole control set (`inverse` aliased); well fg-absolute-24, ink fg-96, active chip fg-08 | **none** — ✅ executed 2026-08-28: bumped, `tone="sunken"` on the cluster, `.icons-ctl` + the active-chip override deleted |
| 🟢 | [CatalogCardFrameAndZoom](outbox/CatalogCardFrameAndZoom.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-28 — component 0.121.0 · theme 0.83.0: catalog card transparent at rest / fg-04 hover · `plateRule={false}` · the zoom reaches any child | **none** — ✅ executed 2026-08-28: bumped, `plateRule={false}` passed, all three blocks deleted |
| 🟢 | [SideNavWidthLadder](outbox/SideNavWidthLadder.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — kol-framework 0.32.0: `--kol-sidenav-w` 264px, 320px from 1536 | **none** — ✅ executed 2026-08-27: bumped, the local two lines deleted, rail verified at 320 on 1920 |
| 🟢 | [MediaLibraryPages](outbox/MediaLibraryPages.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.118.0 · media-client 0.2.0: `MediaLibrary variant="browse"` / `"library"` (r2b2's page in two), `createMediaClient({ buckets })` | **none** — ✅ executed 2026-08-27: Overview → `library`, Browse → `browse`, one three-bucket client, local ColumnBrowser page retired |
| 🟢 | [IconSetCut](outbox/IconSetCut.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — kol-icons 0.24.0: `KOL_ICON_SET_V1_META` + `getCut(name)`, cut derived at build (41 solid · 171 stroke) | **none** — ✅ executed 2026-08-27: bumped, `/icons` reads `getCut`, the SVG glob deleted |
| 🟢 | [ControlToneInverse](outbox/ControlToneInverse.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.117.0 · theme 0.78.0: `tone="inverse"` on ViewToggle (icon) · Dropdown · Input · SearchInput, `ContentFilters tone` forwards | **none** — ✅ executed 2026-08-27: bumped, `tone="inverse"` at `/icons`, local CSS + wrapper retired; the shipped `surface-tertiary` chip was corrected to `fg-16` in theme **0.78.1** the same hour, bumped, nothing local left |
| 🟢 | [ShelfCardTiltWrapsCard](outbox/ShelfCardTiltWrapsCard.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🔵 `filed` · 2026-08-27 — `ParallaxShelf`'s `tilt` is in the media slot, inside `ContentMedia` + `ContentCard work`'s `overflow-hidden`: the frame never leans, and the card's hover `zoom` has been dead on the shelf since 0.12.0 | **none yet** — bump kol-content on return, then eyeball `/work` |
| 🟢 | [TiltBentoVoiceAndDefaults](outbox/TiltBentoVoiceAndDefaults.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🔵 `filed` · 2026-08-27 — `TiltBento`'s subtitle rides `kol-mono-text`, **retired in kol-theme** (0 rules in the built CSS); `titleClassName` / `buttonLabel` defaults differ from the fork's | **none** — all seven tiles pass the two seams explicitly; the subtitle voice is DS-internal |
| 🟢 | [ShelfCardMotion](outbox/ShelfCardMotion.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — kol-content 0.12.0: `ParallaxShelf` default card has `enter` + `tilt` (both default true), the site's `ShelfEnter` verbatim + `TiltCard grounded` | **none** — ✅ executed 2026-08-27: bumped; shelf passes items + `titleClass` only; `ShelfEnter` · `SHELF_HEIGHTS` · `tilt` · `renderCard` · `.work-shelf` rule gone |
| 🟢 | [SectionCtaConnectVariant](outbox/SectionCtaConnectVariant.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.105.0: `SectionCta variant="connect"` shipped | **none** — bumped; swap DECLINED by the user ("leave it"); wrapper kept, renamed `SectionCtaWrapper.jsx` |
| 🟢 | [SectionBackgroundProp](outbox/SectionBackgroundProp.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.103.0: `background` on all six sections (`primary`/`secondary`/`tertiary`/`inverse`/`auto`/`none` or a raw utility), defaults unchanged | **none** — ✅ executed 2026-08-27: bumped, `ConnectCta` forwards `background`, `/work` `none`, `HomeSignup` by prop |
| 🟢 | [WorkCardAndShelf](outbox/WorkCardAndShelf.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.102.3 · content 0.11.0: `ContentCard work` one-line title + `helper-12 uppercase` meta at inverse 80; `ParallaxShelf` renders `ContentCard work` on the ladder, `titleClass` forwarded, caption `kol-eyebrow` | **none** — ✅ executed 2026-08-27: bumped; grid card passes content + face; shelf passes `titleClass` only; `.work-shelf` rule deleted |
| 🟢 | [TagTertiary](outbox/TagTertiary.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — theme 0.70.0 · component 0.102.2: `Tag variant="tertiary"` — secondary's fill, no outline in any state, fg-80, mono per size at 400 / 0.04em / 100% | **none** — ✅ executed 2026-08-27: bumped, `variant="tertiary"`, className + `ui.css` rule deleted |
| 🟢 | [CollectionItemMinWidth](outbox/CollectionItemMinWidth.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.102.1: every collection `<li>` `min-w-0`, list track `minmax(0, 1fr)` | **none** — ✅ executed 2026-08-27: bumped, rule + className deleted |
| 🟢 | [WorkListingRowsAndFilters](outbox/WorkListingRowsAndFilters.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.100.0 · theme 0.69.0: work row as ruled (168 / 16 / fill thumb, no hairline, hover fg-08, typeface-row voices), collection + filters panel declare `container-type`, fluid group pr-12 + `className`/`wrapClassName`, ≤6 values stack by default, `.kol-tag--*` carry type, `thumb="fill"`, `SectionText` headlines balance | **none** — ✅ executed 2026-08-27: bumped, seams + four `ui.css` rules + `stack`/`text-balance` dropped, list swapped onto `ContentCollection` + `ContentRow work`, `WorkListItem` retired |
| 🟢 | [FoundrySpecimenSections](outbox/FoundrySpecimenSections.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — kol-foundry 0.8.0 · component 0.97 · theme 0.66.0: three sections in the package, text-only `SectionCardItem` (hover only), kol-foundry `PairingCard` on the row voices, one dropdown, `fg-08` cells, missing glyphs `fg-24`, borderless variable plate, filters title gap 16 | **none** — ✅ executed 2026-08-27: bumped, `TypefacePage` on the package sections, `ui/Feature*` + `ui/Pairing*` + `ui.css` foundry rules → `_tmp/` |
| 🟢 | [FontPreviewClampAndBearings](outbox/FontPreviewClampAndBearings.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — kol-foundry 0.7.3: clamp back 96/1 · 64/3 · 48/4 · 24/5 with ellipsis; clamped element `padding-inline 0.15em / margin-inline -0.15em`, wrapper no longer clips | **none** — ✅ executed 2026-08-27: bumped, no consumer change |
| 🟢 | [FontPreviewNoClamp](outbox/FontPreviewNoClamp.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — kol-foundry 0.7.2: default `SIZE_LADDER` carries no `lines`, no rung clamps; `lineClamp` stays opt-in | **none** — ✅ executed 2026-08-27: bumped to 0.7.2, no consumer change |
| 🟢 | [TypefaceCardRevealText](outbox/TypefaceCardRevealText.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — kol-foundry 0.7.1: `FOUNDRY_SAMPLE_TEXT` exported, the card reveal renders its first sentence in the face | **none** — ✅ executed 2026-08-27: bumped (0.7.2 carries it), no consumer change |
| 🟢 | [FoundryComponentsReconcile](outbox/FoundryComponentsReconcile.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — kol-foundry 0.7.0: the nine files synced whole from the site's copies (+ `TypefaceAlphabet` exported), router/`SectionTitle` coupling dropped, peers component ≥0.95.0 · theme ≥0.64.0 | **none** — ✅ executed 2026-08-27: bumped, the nine + `TypefaceAlphabet` imported from the package, `onNavigate` on both grid call sites, fork → `_tmp/2026-08-27-foundry-reconcile/` (11 files) |
| 🟢 | [TypefaceCardAndRow](outbox/TypefaceCardAndRow.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — theme 0.64.0 · component 0.95.0: typeface row ink by ramp (name + classification emphasis, year 64), card title = row title, `reveal` on the typeface card | **none** — ✅ executed 2026-08-27: bumped, seams + group-hover wiring dropped, `reveal` adopted, library swapped onto `ContentCollection` + the DS pair, `TypefaceLibraryItem.jsx` retired to `_tmp/` |
| 🟢 | [ContentRowsAndPrintCard](outbox/ContentRowsAndPrintCard.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — theme 0.63.0 · component 0.94.0: square row thumb by rule, no row zoom, `ContentCard print` flip + rect + keyboard, `kol-eyebrow` one name (aliases kept) | **none** — ✅ executed 2026-08-27: bumped, the two `ui.css` rules deleted; kicker → eyebrow renames aliased, at our pace |
| 🟢 | [ContentFiltersTitleGap](outbox/ContentFiltersTitleGap.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.93.1: title `pr-4` by rule | **none** — ✅ executed 2026-08-27: bumped, local `pr-4` dropped |
| 🟢 | [SectionHeroFootGap](outbox/SectionHeroFootGap.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.92.0: foot inset = overlap + 32/48 | **none** — ✅ executed 2026-08-27: bumped, translate dropped |
| 🟢 | [StackCardHover](outbox/StackCardHover.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — theme 0.61.0 · component 0.90.2 · content 0.10.2: hero zoom rung 1.02, article title dim on card hover | **none** — ✅ executed 2026-08-27: bumped, local rules + hooks deleted; `/stack` swap complete |
| 🟢 | [SectionTextBodyMono](outbox/SectionTextBodyMono.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — theme 0.60.0: section body = mono-16, sans opt-in | **none** — ✅ executed 2026-08-27: bumped, Stack hero `bodyClass` dropped |
| 🟢 | [SectionNewsletterForm](outbox/SectionNewsletterForm.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.90.1: form fills its wrapper (520 at desktop), `bodyClass` override gone | **none** — ✅ executed 2026-08-27: bumped |
| 🟢 | [SectionNewsletter](outbox/SectionNewsletter.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.90.0: `SectionNewsletter` on the ladder + cap, `NewsletterBand` aliased | **none** — ✅ executed 2026-08-27: `HomeSignup` on it, `bg-fg-inverse-04` |
| 🟢 | [ListingCardHoverZoom](outbox/ListingCardHoverZoom.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — kol-content 0.10.1: ListingCard on the family's zoom rule | **none** — ✅ executed 2026-08-27: bumped |
| 🟢 | [ContentCollectionColsResponsive](outbox/ContentCollectionColsResponsive.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.89.0: `cols` breakpoint map | **none** — ✅ executed 2026-08-27: `/stack` on `{ md: 3, xl: 4 }` |
| 🟢 | [ListingCardThumbBorder](outbox/ListingCardThumbBorder.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — content 0.10.0 + component 0.88.0: `frame` on ListingCard, article thumb frame off by default | **none** — ✅ executed 2026-08-27: bumped, `frame={false}` dropped |
| 🟢 | [ContentCollectionCols](outbox/ContentCollectionCols.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.87.0: `cols={N}` on the collection's grid form | **none** — ✅ executed 2026-08-27: `/stack` on `cols={3}` |
| 🟢 | [SectionHeroFootClearance](outbox/SectionHeroFootClearance.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.86.0: end-justified content pads for the foot overlap | **none** — ✅ executed 2026-08-27: bumped, Stack back on the 250 default |
| 🟢 | [DisplayTightRamp](outbox/DisplayTightRamp.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — theme 0.59.0: display-01…04 are Tight 500, `display-lg`/`-section`/`-section-sm`/`-subsection` alias onto 01/02/03/03 | **none** — ✅ executed 2026-08-27: 19 swaps, zero elder type classes in web |
| 🟢 | [SectionSplitMediaBounded](outbox/SectionSplitMediaBounded.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.85.0: media frame = rung − padding, width follows ratio; four rungs, four heights | **none** — ✅ executed 2026-08-27: bumped, process split on 40 |
| 🟢 | [SectionHeightForty](outbox/SectionHeightForty.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.84.0: `40` on the shared ladder, default stays 60 | **none** — ✅ executed 2026-08-27: bumped, Studio process split `height="40"` |
| 🟢 | [SectionHeightLadder](outbox/SectionHeightLadder.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.83.0: one `sectionHeights` map on cards · faq · cta, default 60 | **none** — ✅ executed 2026-08-27: bumped, nothing passed |
| 🟢 | [SectionSplitHeight](outbox/SectionSplitHeight.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.82.0: `SectionSplit height` ladder, default 60 (split only) | **none** — ✅ executed 2026-08-27: bumped, both splits on the default; cards · faq · cta → `SectionHeightLadder` |
| 🟢 | [ThemeToggleLabelTarget](outbox/ThemeToggleLabelTarget.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — framework 0.27.1: the toggle's visible label names the target mode | **none** — ✅ executed 2026-08-27: bumped, nothing to change |
| 🟢 | [SectionThemeInverse](outbox/SectionThemeInverse.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-27 — component 0.81.0: `theme` inverse / light / dark on hero · split · cards, one `data-theme` scope on the root | **none** — ✅ executed 2026-08-27: bumped, Studio hero `theme="inverse"` |
| 🟢 | [SectionHeroSplitHeight](outbox/SectionHeroSplitHeight.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — component 0.80.1: literal `SPLIT_HEIGHTS`, media half `h-full min-h-0` | **none** — ✅ executed 2026-08-26: bumped, nothing passed |
| 🟢 | [SectionHeroNoOverrides](outbox/SectionHeroNoOverrides.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — component 0.80.0: hero renders SectionText bare; caps is a molecule role (`headlineCase`) | **none** — ✅ executed 2026-08-26: bumped, nothing passed |
| 🟢 | [SectionRevealSeams](outbox/SectionRevealSeams.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — component 0.79.0 + content 0.9.3: `slotClass`/`slotStyle` on SectionText (forwarded everywhere), `itemClassName`/`itemStyle` on SectionCards, `partClassName`/`partStyle` on ArticleHeader | **none** — ✅ executed 2026-08-26: all six surfaces re-stamped from the retired locals' delays, Home foundry per-element |
| 🟢 | [SectionCtaEditorial](outbox/SectionCtaEditorial.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — component 0.76.4: inner cap 1600, row labels on the set eyebrow | **none** — ✅ executed 2026-08-26: bumped (+ theme 0.58.0), no call-site change |
| 🟢 | [SectionSplitMediaClip](outbox/SectionSplitMediaClip.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — component 0.76.3: `mediaClip` seam on the split | **none** — ✅ executed 2026-08-26: bumped, `HomeFoundry` on `SectionSplit mediaClip={false}` |
| 🟢 | [SectionTextLabelVoice](outbox/SectionTextLabelVoice.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — component 0.76.2 + theme 0.57.1: one eyebrow voice (the hero's) across the set; uppercase stays by role | **none** — ✅ executed 2026-08-26: bumped, no call-site change |
| 🟢 | [SectionHeroCarouselSeams](outbox/SectionHeroCarouselSeams.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — component 0.76.1: the carousel branch forwards renderTitle / ctaLabel / onNavigate / show* / options | **none** — ✅ executed 2026-08-26: `/foundry` on `SectionHero media=[…] height="80"`, `FeaturedCarousel` import gone (zero call sites) |
| 🟢 | [SectionHeroRound2](outbox/SectionHeroRound2.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — component 0.76.0 + theme 0.57.0: vh presets · `justify="end"` · `veil` · `foot`/`overlap` · array media · text-only · `SectionCta variant="centered"` | **none** — ✅ executed 2026-08-26: Studio `full` + scrim, Stack on the hero (`StackHero` retired), typeface `60`, licensing text-only hero + `SectionFaq`, `FoundryCTA` ×3 → `SectionCta`; `/foundry` waits on `SectionHeroCarouselSeams` |
| 🟢 | [FeaturedCarouselFullWidth](outbox/FeaturedCarouselFullWidth.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — framework 0.25.0 + component 0.70.0: `fullWidth` stamps `is-full`, slide `width: 100%`, off-stage videos pause | **none** — ✅ executed 2026-08-26: bumped in the same install; studio hero at 1440 eyeball is the user's |
| 🟢 | [FrameworkComponentPeer](outbox/FrameworkComponentPeer.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — framework 0.25.0 · component 0.70.0 · styleguide 0.2.0: every DS package declares the tier as peers; class closed | **none** — ✅ executed 2026-08-26: framework ^0.25.0 + component ^0.70.0 in both apps, workspace override deleted, `pnpm why` → ONE version on peers alone |
| 🟢 | [SectionSet](outbox/SectionSet.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — component 0.71.0 + theme 0.54.0: `SectionText` + `SectionHero` / `SectionSplit` (+`align`) / `SectionCards` / `SectionCta` / `SectionFaq`, old names aliased | **none** — ✅ executed 2026-08-26: bumped, Studio on the set (hero · split · cards · cta), `StudioAboutCard` → `_tmp/`; hero eyeball + the "Services" kicker copy are the user's |
| 🟢 | [MediaLibraryReconcile](outbox/MediaLibraryReconcile.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — component 0.69.0 + theme 0.53.0: page variant = FileList's read-only render, provider prefix-scoped (BREAKING for `useMediaLibrary()` only) | **none** — ✅ executed 2026-08-26: component ^0.69.1 + theme 0.53.0 in both apps, one kol-component in the tree (via the FrameworkComponentPeer override); `/library` eyeball vs media.kolkrabbi.io is the user's |
| 🟢 | [HeadingTwoNarrow](outbox/HeadingTwoNarrow.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-12 — theme 0.40.0, heading-02 is narrow | **none** — ✅ theme 0.40.0 in; hero/takeover eyeball is the user's |
| 🟢 | [CardFeatureHoverZoom](outbox/CardFeatureHoverZoom.md) | **kol-ds-ui** — same ledger | 🟢 `closed` · synced 2026-08-12 — theme 0.40.0 + component 0.38.0, 1.03 zoom on the visual | **none** — ✅ adopted across all FOUR fork consumers (Home·Studio·foundry·WorkshopFeatures), fork pair retired, data → `src/data/featureCards.js` |
| 🟢 | [ShellNavItemInk](outbox/ShellNavItemInk.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-12 — theme 0.39.0: ink 80, active +300, `--own` 500→300 was the rails split | **none** — ✅ theme 0.39.0 in (bump-everything wave); live rail eyeball is the user's |
| 🟢 | [NavLinkUnderline](outbox/NavLinkUnderline.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-12 — `.kol-link-underline` shipped in theme 0.35.0 | **none** — ✅ executed same session: theme 0.35.0 in, both call sites swapped, local block deleted |
| 🟢 | [RecordManager](outbox/RecordManager.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-09 — shipped in **component 0.25.0** | **none** — ✅ CLOSED as superseded 2026-08-14 (user: "close it"); Library adopted MediaLibrary 08-01, SlideDeckManager has no editable state to justify the organism |
| 🟢 | [SideNavGrabResize](outbox/SideNavGrabResize.md) | **kol-ds-ui** — same ledger | 🟢 `closed` · synced 2026-08-09 — shipped in **framework 0.14.0** | **none** — ✅ executed 2026-08-09: brand adopted the package SideNav wholesale, fork + Button toggle retired to `_tmp/`, collapse CSS reduced to the rail plane |
| 🟢 | [ListingCardSpec](outbox/ListingCardSpec.md) | **kol-ds-ui** — same ledger | 🟢 `closed` · synced 2026-08-15 — kol-content 0.7.0: `ListingCard` rename, `readmore` gone, geometry conformed (WorkCard deliberately not aliased — different card, folds next major) | **none** — ✅ executed same session: ^0.7.0 in, both call sites on `ListingCard`, locals already retired |
| 🟢 | [tone-of-voice-skill-and-agent](outbox/tone-of-voice-skill-and-agent.md) | **dotfiles** — `~/.dotfiles/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-06 | **none** — skill + agent shipped and harness-verified; the de-clienting rewrite the audit triggered also shipped (4 files, user-ordered) |
| 🟢 | [SideNavMobilePosition](outbox/SideNavMobilePosition.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — framework 0.23.0: the aside's box lives in `.kol-sidenav`, the 767px drawer rule wins | **none** — ✅ executed 2026-08-26: framework ^0.23.0 in, stopgap block deleted, brand re-walked at 393 (aside fixed −280, content top 0); the re-walk's two finds fixed same pass (Gallery header clears the hamburger, `/assets` ledes wrap their path tokens) |
| 🟢 | [TableMobileScroll](outbox/TableMobileScroll.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — nothing built: `.kol-table-wrapper` has always scrolled (measured ×4 on brand production at 393); the hidden scrollbar + headless no-drag hid it | **none** — touch affordance ruled 2026-08-26 at the DS: none, the cut columns are the affordance |
| 🟢 | [CodeBlockMobileOverflow](outbox/CodeBlockMobileOverflow.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — component 0.68.1 + theme 0.50.2: the block wraps, a chipless first line reserves the copy control's lane | **none** — ✅ executed 2026-08-26: component ^0.68.1 + theme 0.51.0 in; verified on the docs reader at 393 (pre-wrap, 311/311, 44px lane) — only once `pnpm.overrides` forced one kol-component (see `NestedDsDependencies`); `/stack/vcap` on a phone = production check |
| 🟢 | [WorkshopShellMobile](outbox/WorkshopShellMobile.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — framework 0.23.0: active tab scrolled into view, snap on flick; the two-up cards are THIS repo's rows | **none** — ✅ executed 2026-08-26: six `DashboardComponents.jsx` rows stack below `md` (393 column · 1280 row); the tab scroll only fired once `pnpm.overrides` gave kol-workshop the real framework 0.23.0 (it nested 0.20.1) — Dashboard tab lands 79–177 in the strip |
| 🟢 | [MobileTouchFloor](outbox/MobileTouchFloor.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — RULED: no type floor, 24px hit floor (WCAG 2.5.8 AA), drawn size never moves; theme 0.51.0 lifts ToggleSwitch + Slider | **none** — ✅ executed 2026-08-26: theme 0.51.0 in; AssetTable buttons 16→24 box (glyphs 16/8 untouched), Reference + Gallery links carry a 24px pseudo-element extent, hit-tested ±5px at 393 |
| 🟢 | [NestedDsDependencies](outbox/NestedDsDependencies.md) | **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md` | 🟢 `closed` · synced 2026-08-26 — workshop 0.23.0 · chess 0.7.0 · content 0.9.0 · foundry 0.6.0 · store 0.2.0: the DS tier is a `peerDependencies` floor in all five | **none** — ✅ executed 2026-08-26: five bumped (+ framework ^0.24.0, theme 0.52.1), `pnpm.overrides` block deleted, `pnpm why kol-component` → ONE version; workshop tabs + CodeBlock re-verified at 393 |

## History

| 2026-08-30 | **"Make foundry look like work/prints" took FOUR tickets for what was two values.** `TypefaceRowSkin` (0.138.0) → resolved by deriving the whole box, which moved `pad` and `rung` uninvited and could not reach `FILL`; `TypefaceRowSkinCorrection` (0.139.0) → undid the derive; `TypefaceCardRestSkin` (0.140.0) → the same two values on the card, filed with an explicit DO-NOT-DERIVE; `TypefaceRowHoverBg` (0.141.0) → the leftover `hover` bg wash, which no earlier ticket mentioned. **Two lessons, both on the tickets: when the ask is two values the ticket says two values, and when a user says "make X look like Y", diff EVERY state — rest, hover, selected, focus — not the one in the screenshot.** |
| 2026-08-30 | **`PageGutterOwnership` filed to kol-ds-ui off a full route audit.** The user spotted a gutter difference between `/work` and `/foundry` on a skim; measuring every public route at 1280 and 1920 found three different gutter sources, two content widths at one breakpoint (1752 vs 1800), the two foundry pages 48px apart on identical container classes, and row variants padding again on top (16 vs 24). Four routes carry no ruled container at all — ours to adopt once the model is ruled. Report: `plans/2026-08-30-gutter-and-container-audit.md` |
| 2026-08-29 | **`RowVariantNamesAndSpecs` filed to kol-ds-ui.** Putting `/prints`' list on `ContentRow variant="work"` exposed the naming defect: `print` and `catalog` are byte-identical boxes, and the variant that fit prints was named for another page. User ruled the names, not the geometry. Filed with two riders — a `specs` slot so print fields need no seventh variant, and a request for a current component+name overview because the showcase page is stale. `/prints` carries `ratio="1 / 1.41421"` meanwhile |
| 2026-08-28 | **The motion sheet localised — a return this repo never filed.** kol-ds-ui acted on a user ruling and minted kol-theme's `kol-animation.css`: the reveal family and `.animatedWord`'s rest state left `apps/web/src/styles/animations.css` (bare names aliased, so nothing here migrated), and the second one closed a real DS defect — `atoms/AnimatedTitle` had been animating FROM a state only this app's stylesheet declared, so it rendered static in every other repo. Bumped theme 0.89.0 · component ^0.128.0, `animations.css` 127 → 73 lines, and `components/ui/AnimatedTitle.jsx` — a 75-line fork of the DS's 116-line component with a strictly smaller prop set — retired to `_tmp/2026-08-28-animated-title-fork/`. ⚠ `.animate-on-scroll` / `.animate-in` survives with zero call sites and an `!important` |
| 2026-08-28 | **All four `/icons` tickets returned and were consumed the same session.** component 0.121.1 · theme 0.83.0 · framework 0.35.0 · shell 0.15.0, one copy verified. `tone="sunken"` is the control set's name and reaches Button · IconFrame · ThemeToggle; the catalog card is frameless at rest with `plateRule={false}`; `PageHeader` takes `actions` on the lede's baseline plus `subtitleMaxWidth`; the icon well is inset so `gap-N` means N. **`apps/brand/src/styles/controls-tone.css` — six local rules at its peak — is retired to `_tmp/2026-08-28-icons-controls-tone/` and its `@import` is out of `index.css`: `/icons` carries no local CSS at all.** ⚠ Owed his eyes on a restarted server; DS ceiling noted — the inset well bleeds 4px past a row's outer edge when the toggle is first or last |
| 2026-08-28 | **`PageHeaderTrailingSlot` + `ViewToggleWellGap` filed to kol-ds-ui** — the rest of the `/icons` session. `PageHeader` has no trailing slot and flexbox exposes only a column's first baseline, so putting a control on the lede's line meant pulling the subtitle out of the component and killing its inline margin with `!important`. `ViewToggle variant="icon"`'s `p-1` well makes `gap-4` render 20 / 16 around it |
| 2026-08-28 | **`ControlToneSunken` + `CatalogCardFrameAndZoom` filed to kol-ds-ui** off brand `/icons`. The control tone is misnamed and unreachable by half the set (`Button` · `IconFrame` · `ThemeToggle` take no `tone`) — user ruled the name **`sunken`**, active chip `--kol-fg-08`. The catalog card wants its frame off at rest with the old rest value as the hover, a switchable plate hairline, and a zoom that reaches non-`img` media. Six rules carried meanwhile in `apps/brand/src/styles/controls-tone.css` |

| Date | Event |
|---|---|
| 2026-08-27 | **`/icons` is the native gallery + `ControlToneInverse` filed.** `/icons/shipped` rebuilt on the app-tier catalog pattern (kol-shell PageHeader → ContentFilters → ContentCollection ×6 → ContentCard catalog, square glyph media, icon ViewToggles + size Dropdown) and promoted to `/icons` (the DS-showcase embed painted its own ground over the new wash; `/icons/shipped` redirects). `BrandLayout` took `pageWash` — AppShell's name and variable — set to `fg-04`, home exempt. The wells' grey read as a plate on the wash → inverse tone carried locally (two rules) and filed for ViewToggle · Dropdown · Input |
| 2026-08-27 | **`TiltFamilyForks` executed and the queue is back to ZERO — and it surfaced two DS defects, both filed.** The bump wave (component 0.108.0 → **0.110.0**, icons 0.22.0, theme 0.72.0) carried the Tilt family in: home Foundry + WorkDetail onto the DS `TiltCard`, the seven highlight tiles onto `TiltBento`, three local forks retired to `_tmp/`. Filed back: `TiltBentoVoiceAndDefaults` (the subtitle's class is retired in the theme — measured 0 rules in this app's built CSS) and `ShelfCardTiltWrapsCard` — the user's report that `/work`'s shelf does not tilt, root-caused to `ShelfCardMotion` putting the tilt inside the card's own clip box, which has also been silently killing the work card's hover zoom |
| 2026-08-27 | **`TiltFamilyForks` filed from kol-ds-ui** — the three local tilt forks swap onto the DS Tilt family (kol-component 0.110.0: `BentoCard` → `TiltBento`, `TiltCard` + `useTilt` as shipped) |
| 2026-08-27 | **`ShelfCardMotion` came back 🟢 (kol-content 0.12.0), executed** — the shelf renders the DS default card (enter + tilt); `Work.jsx` carries no shelf card code, `ui.css` no `.work-shelf` rule |
| 2026-08-27 | **`ShelfCardMotion` filed to kol-ds-ui** — the shelf card's entrance (`ShelfEnter`) + `TiltCard grounded` into the DS shelf card; the caption is already 0.11.0's. Local until it ships: `renderCard` + the rule |
| 2026-08-27 | **`SectionCtaConnectVariant` came back 🟢 (component 0.105.0) — bumped, swap declined by the user.** `ConnectCta.jsx` renamed `SectionCtaWrapper.jsx` (four call sites) so the name says what it is |
| 2026-08-27 | **`SectionCtaConnectVariant` filed to kol-ds-ui** — the contact CTA becomes a `SectionCta` variant; `ConnectCta.jsx` (four call sites) retires on the return (user: a prefix family we opt out of at the first opportunity is stupid) |
| 2026-08-27 | **`SectionBackgroundProp` came back 🟢 (component 0.103.0), executed** — sections take `background` as a prop; `/work` CTA `none`, `HomeSignup` its rung by prop; no `className` surfaces left |
| 2026-08-27 | **`SectionBackgroundProp` filed to kol-ds-ui** — the family's surface as a prop (user). Local: `ConnectCta` forwards `className`, `/work` CTA on `bg-transparent`; the shelf card's entrance + tilt restored locally (`ShelfEnter`, `TiltCard` shelf-only) — DS ask pending with the `/work` cleanup |
| 2026-08-27 | **`WorkCardAndShelf` came back 🟢 (component 0.102.3 · content 0.11.0), executed — `/work` fully on the DS: LIST rows, GRID cards and the shelf all `ContentCard`/`ContentRow work`, the page passes content + its title face. Zero `.work-*` CSS beyond the two faces |
| 2026-08-27 | **`WorkCardAndShelf` filed to kol-ds-ui** — the `/work` card values (one-line title, uppercase helper meta at inverse 80), the shelf rendering the same DS card on its three-height ladder, eyebrow captions. Local same pass: one `WorkCard` for GRID + shelf, Typefaces shelf shows all five faces (placeholders for the three without projects), Tools + Systems one shelf, 50vh under the last |
| 2026-08-27 | **`TagTertiary` came back 🟢 (theme 0.70.0 · component 0.102.2), executed** — `/work` rows on `<Tag variant="tertiary">`, nothing else passed; local override gone |
| 2026-08-27 | **`TagTertiary` filed to kol-ds-ui** — the `/work` row tags ruled as a new variant (no outline, mono 10 at 400 / 0.04em / 100%, fg-80, `#` on); local as secondary + override until it ships |
| 2026-08-27 | **`CollectionItemMinWidth` came back 🟢 (component 0.102.1), executed** — bumped; the local `min-width: 0` rule gone. `/work` LIST carries zero local CSS again |
| 2026-08-27 | **`CollectionItemMinWidth` filed to kol-ds-ui** — first look at `/work` LIST on the DS row: the collection item sized to the nowrap description (3586px in 1232), type/year off-screen. Local `min-width: 0` on the items until it ships |
| 2026-08-27 | **`WorkListingRowsAndFilters` came back 🟢 (component 0.100.0 · theme 0.69.0), executed — `/work` LIST + GRID on the content-card system.** `ContentFilters` (LIST/GRID) → `ContentCollection` → `ContentRow work` (content + the `work-display-preview` face only) / `ContentCard work` (`work-display-title` face, client-or-type · year); `WorkListItem` import + the `ListRows` stagger gone; zero `.work-row*` / `.work-filters*` CSS. Shelf stays on the navbar toggle |
| 2026-08-27 | **`WorkListingRowsAndFilters` filed to kol-ds-ui** — every `/work` ask that had no prop path, written down instead of carried in chat: the row's ruled values, the dead container query, the filters' edge/seam/narrow default, Tag size type, fill thumb, balanced headlines |
| 2026-08-27 | **`FoundrySpecimenSections` came back 🟢 (kol-foundry 0.8.0 · component 0.97 · theme 0.66.0), executed — the foundry fork is fully gone.** `components/sections/foundry/` holds only `TypefacePage`, `FoundryOtherTypefaces`, `InDevelopmentSection`; `ui/Feature*`, `ui/Pairing*`, `.foundry-title`/`.pairing-card`/`.feature-card` → `_tmp/2026-08-27-foundry-specimen-sections/`. Same pass: `/work` bar gets LIST/GRID, a TEMP DS `ContentRow work` sits first in the list beside the local rows, GRID is all DS `ContentCard work` |
| 2026-08-27 | **`FoundrySpecimenSections` filed to kol-ds-ui** after the Málrómur slug-page pass. Local same pass: image-wrapper borders off (`TypefacePage` ×4), `PairingCard` on the typeface row voices + 96px divider, `FeatureGrid` persistent selected state removed (hover only); `/dev/demo` + its five components retired to `_tmp/2026-08-27-dev-demo-retired/` |
| 2026-08-27 | **`FontPreviewClampAndBearings` came back 🟢 (kol-foundry 0.7.3), executed** — bumped; the ladder clamps again with the ellipsis, italic side bearings have em-scaled room. No consumer change |
| 2026-08-27 | **`FontPreviewClampAndBearings` filed to kol-ds-ui** — `FontPreviewNoClamp` was my misreading of "the sample is cutting"; the clamp was correct (production) and returns, the real defect is the italic glyphs' side bearings clipped by `overflow: hidden` at the box edges |
| 2026-08-27 | **`TypefaceCardRevealText` (0.7.1) + `FontPreviewNoClamp` (0.7.2) came back 🟢, executed** — kol-foundry bumped to 0.7.2; the card reveal reads the Icelandic sample, Font Preview renders the passage whole. No consumer change |
| 2026-08-27 | **`FontPreviewNoClamp` filed to kol-ds-ui** — the typeface pages' Font Preview clamps the sample to N lines per size rung and ellipsises it; the passage renders whole (user: "the sample is cutting") |
| 2026-08-27 | **`TypefaceCardRevealText` filed to kol-ds-ui** — the GRID card's hover reveal carries the English pangram from the retired local item; the foundry's Icelandic sample is the text (user), one export in kol-foundry |
| 2026-08-27 | **`FoundryComponentsReconcile` came back 🟢 (kol-foundry 0.7.0), executed — the foundry fork is gone.** `TypefacePage`, `FoundryTypefaces`, `FoundryOtherTypefaces`, `FoundryOpentypeFeatures`, `FoundryTypefacePairing`, `FoundryTypefaceDetails`, `GlyphInspector`, `Demo` import the nine from `@kolkrabbi/kol-foundry`; both grid call sites pass `onNavigate`; 11 local files → `_tmp/2026-08-27-foundry-reconcile/`. `components/sections/foundry/` keeps its 11 page-level files (no package counterpart) |
| 2026-08-27 | **`FoundryComponentsReconcile` filed to kol-ds-ui** — `components/sections/foundry/` is a fork of `kol-foundry` (nine same-named files, eight drifted forward here); the package syncs to the local copies, then the site imports the package and the fork retires |
| 2026-08-27 | **`TypefaceCardAndRow` came back 🟢 (theme 0.64.0 · component 0.95.0), executed — typeface library swapped onto the DS pair.** `ContentCollection cols={{md:2,lg:4}}` of `ContentCard typeface` (glyph in `media`, pangram in `reveal`) / list of `ContentRow typeface` (alphabet in `footer`); no `*Class` seams left; `TypefaceLibraryItem.jsx` → `_tmp/2026-08-27-typeface-library-ds-swap/`. Same pass: `/foundry` hero on the studio's split inverse hero |
| 2026-08-27 | **`TypefaceCardAndRow` filed to kol-ds-ui** — the typeface library's DS card/row, tuned locally against `TypefaceLibraryItem` and approved: row name + classification full ink, year 64; card title on the row's voice; card hover fades plate + glyph out, pangram in (asked as a `reveal` slot) |
| 2026-08-27 | **`ContentRowsAndPrintCard` came back 🟢 (theme 0.63.0 · component 0.94.0), executed** — bumped both apps, the two local `ui.css` rules (square row thumb, no row zoom) deleted; `/prints` complete on the set. Eyebrow renames aliased, not done |
| 2026-08-27 | **`/prints` on the set locally + `ContentRowsAndPrintCard` filed.** Prints: `ContentFilters` (Category narrow · Year, LIST/GRID), `ContentCollection cols={{md:2,lg:4}}`, `ContentCard print` / `ContentRow article` identical to Stack's rows; `PrintGridCard` import gone from the grid route. Two local `ui.css` rules (square row thumb, no row zoom) carry a delete-on-return note |
| 2026-08-27 | **`ContentFiltersTitleGap` came back 🟢 (0.93.1), executed** — every `ContentFilters` bar centres its divider now, no consumer class |
| 2026-08-27 | **`ContentFiltersTitleGap` filed to kol-ds-ui.** `/stack` filter panel: `Type` group (research / standard, `stack: true` = narrow) left of `Tags`; `type` pulled by the blog query |
| 2026-08-27 | **`SectionHeroFootGap` came back 🟢 (0.92.0), executed** — Stack hero text sits 48px above the card by rule; no local nudge left. 0.91.3 (featured card verbatim) rode along |
| 2026-08-27 | **`SectionHeroFootGap` filed to kol-ds-ui** with the tuned value from `/stack` (translate 176 / 96) |
| 2026-08-27 | **component 0.91.0 — `ContentCard variant="article" hero`.** The DS's own follow-up to the Stack swap: the featured card had been `ListingCard size="hero"` with no content-card equivalent ("we have been updating the wrong card"). Stack's featured card is on `ContentCard hero` now; the last `ListingCard` import in web is gone |
| 2026-08-27 | **`StackCardHover` came back 🟢, executed — `/stack` content-filter swap COMPLETE.** Hero (`SectionHero` 90vh, wash, veil, foot), featured `ListingCard` (no frame, 1.02 zoom), `ContentFilters` bar with LIST/GRID, `ContentCollection cols={{md:3,xl:4}}` of `ContentCard`/`ContentRow` article (uppercase Tight titles, truncated, mono ledes, no tags/date, title dim), `SectionNewsletter` on `bg-fg-absolute-16`, `SectionCta`. Zero local CSS left for it |
| 2026-08-27 | **`StackCardHover` filed to kol-ds-ui** with the values tuned locally on `/stack` (hero zoom 1.02, list-card title dim 70%). New order from the user: get the look right here first, then file — the local rules carry a delete-on-return note |
| 2026-08-27 | **`SectionTextBodyMono` came back 🟢 (theme 0.60.0), bumped** — every section lede is mono-16 now |
| 2026-08-27 | **`SectionTextBodyMono` filed to kol-ds-ui** — user ruling: mono is the card body voice, sans opt-in. Newsletter surface on `bg-fg-absolute-16` |
| 2026-08-27 | **`SectionNewsletterForm` came back 🟢 (0.90.1), bumped** — newsletter field 520 wide, molecule bare; surface `bg-fg-absolute-08` (user) |
| 2026-08-27 | **`SectionNewsletterForm` filed to kol-ds-ui.** Newsletter surface back on `bg-fg-04` — `bg-fg-inverse-04` is invisible in dark (4% dark on dark) |
| 2026-08-27 | **`SectionNewsletter` came back 🟢 (0.90.0), executed** — the newsletter is a section-family card now; Home + Stack on it, surface `bg-fg-inverse-04`. Same pass: page gutters off the mains on Stack / Studio / Home / Licensing, kept on the local blocks |
| 2026-08-27 | **`SectionNewsletter` filed to kol-ds-ui.** Also seen on `/stack`: `main`'s `breakpoint-padding` insets every section, so a shaded section can't reach the viewport edges — the pages built from sections should drop the wrapper padding and let the sections gutter themselves |
| 2026-08-27 | **`ListingCardHoverZoom` came back 🟢 (kol-content 0.10.1), bumped** |
| 2026-08-27 | **`ListingCardHoverZoom` filed to kol-ds-ui.** Same pass on `/stack`: LIST/GRID strip on the bar in the RECENT/SAVED voice (helper-14, uppercase labels); hero text pulled back to just above the card (`--kol-section-pb` 2rem) after the DS's foot clearance over-lifted it |
| 2026-08-27 | **`ContentCollectionColsResponsive` came back 🟢 (0.89.0), executed** — `/stack` three from md, four from xl; LIST/GRID strip wired on its filter bar (missed in the card swap) |
| 2026-08-27 | **`ContentCollectionColsResponsive` filed to kol-ds-ui.** `/stack` article cards restored to live's look on the content-card system: kicker `kol-card-kicker`, title `display-03 uppercase`, body clamp 2, no tags, no date |
| 2026-08-27 | **`ListingCardThumbBorder` came back 🟢 (content 0.10.0 · component 0.88.0), executed** — no thumb hairlines on `/stack`, hero card included |
| 2026-08-27 | **`ContentCollectionCols` came back 🟢 (0.87.0), executed** — `/stack` says three across |
| 2026-08-27 | **`ListingCardThumbBorder` filed to kol-ds-ui.** `/stack` list cards on `frame={false}` now; the hero card's thumb border waits on kol-content |
| 2026-08-27 | **`ContentCollectionCols` filed to kol-ds-ui.** Same pass: `/stack` list on `ContentCollection` + `ContentCard`/`ContentRow` article; 31 hardcoded 1400/1600/1800 caps in web → `--kol-container-max`; Stack hero ramp var overridden to sit the text like live |
| 2026-08-27 | **`SectionHeroFootClearance` came back 🟢 (0.86.0), executed** — Stack hero on the default overlap, lede clears the card |
| 2026-08-27 | **`DisplayTightRamp` came back 🟢 (theme 0.59.0), executed** — one display face (Tight 500) on the numbered ramp; every elder display and dead heading class in web swapped to a numbered role. Stack title on `display-01` |
| 2026-08-27 | **`SectionHeroFootClearance` filed to kol-ds-ui.** Stack hero vs live: title `display-02` + uppercase (Tight face pending `DisplayTightRamp`), lede `kol-mono-16` in a 720 column; the 120 overlap is a workaround until the DS pads for the foot |
| 2026-08-27 | **`DisplayTightRamp` filed to kol-ds-ui.** Same pass on Stack: hero `h-[90vh]`, `fullBleed`, image focus centre, wash 80, foot overlap 120 (lede was under the Featured card), title uppercase by role |
| 2026-08-27 | **`SectionSplitMediaBounded` came back 🟢 (0.85.0), bumped** — the split's media frame is sized by the rung now; at 40 the frame is rung − 256px padding, flagged |
| 2026-08-27 | **`SectionSplitMediaBounded` filed to kol-ds-ui** — 40 looked like 60 on the process split because the square item outgrows every rung. Same pass: Stack button removed from the process card (user) |
| 2026-08-27 | **`SectionHeightForty` came back 🟢 (0.84.0), bumped; Studio process split on 40** |
| 2026-08-27 | **`SectionHeightForty` filed to kol-ds-ui.** Same pass: `ProfileCard` lg-h fills the split's media slot and owns its square (photo crops, height never moves when the panel opens); Studio hero copy per the user |
| 2026-08-27 | **`SectionHeightLadder` came back 🟢 (0.83.0), bumped** — the whole section family now sizes on one ladder |
| 2026-08-27 | **`SectionSplitHeight` came back 🟢 (0.82.0) as the split only; `SectionHeightLadder` filed for cards · faq · cta.** Bumped; both splits stand on 60 |
| 2026-08-27 | **`SectionSplitHeight` filed to kol-ds-ui** — the split gets the family's `full`/`80`/`60` as min-height. Same pass: Studio hero copy per the user — label "Kolkrabbi Vinnustofa", headline "Design studio & Atelier based in Reykjavík", body + button gone; framework 0.27.1 (toggle label) in |
| 2026-08-27 | **`ThemeToggleLabelTarget` came back 🟢 (framework 0.27.1), bumped** |
| 2026-08-27 | **`ThemeToggleLabelTarget` filed to kol-ds-ui** — the takeover's theme button should read the mode it switches to. Same pass: framework 0.27.0 bumped (user) |
| 2026-08-27 | **`SectionThemeInverse` came back 🟢 (0.81.0), executed** — Studio split hero on `theme="inverse"` |
| 2026-08-27 | **`SectionThemeInverse` filed to kol-ds-ui** — paired-theme inversion as a prop on the three section organisms; Studio hero first |
| 2026-08-26 | **`SectionHeroSplitHeight` came back 🟢 (0.80.1), bumped** — the split hero holds its preset height now |
| 2026-08-26 | **`SectionHeroSplitHeight` filed to kol-ds-ui.** Studio hero tried on `variant="split"` (user) and collapsed to ~175px: the split's height classes only exist after a runtime `h-`→`min-h-` rewrite, so Tailwind never emits them (verified in the built CSS). Same pass: the hamburger's redundant `Toggle menu` tooltip removed from `Navbar.jsx` (user) |
| 2026-08-26 | **`SectionHeroNoOverrides` came back 🟢 (0.80.0), bumped.** Hero and split now share one text voice with nothing passed at the call site |
| 2026-08-26 | **`SectionHeroNoOverrides` filed to kol-ds-ui** — the hero's remaining per-slot overrides on `SectionText` (body 14px vs the split's 16, capped headline, `gap-6`), seen side by side on Studio. Agent had read the override on first open and not flagged it |
| 2026-08-26 | **`SectionRevealSeams` came back 🟢 (component 0.79.0 · content 0.9.3) and the reveal is back** on Home / Studio / in-development cards, Stack article header, Studio process split, Stack hero, Home foundry per-element — the delays the retired locals carried. 0.78.0 (one container cap across the section family, the user's ruling at the DS) rode along |
| 2026-08-26 | **`SectionRevealSeams` filed to kol-ds-ui.** Six surfaces lost their `.reveal` entrance across the 08-12 / 08-15 / 08-26 adoptions (cards ×3, Stack article header, Studio process split, Stack hero) + Home foundry degraded to one block — each logged as an eyeball owed, none acted on. Reveal stays in `animations.css`; the DS gets seams |
| 2026-08-26 | **`SectionCtaEditorial` came back 🟢 (0.76.4), bumped with theme 0.58.0.** A persistent watch now polls the registry + the outbox every 60s so returns and bumps surface mid-session instead of on the next read |
| 2026-08-26 | **`SectionCtaEditorial` filed to kol-ds-ui** — the `/ CONNECT` band's width and row-label voice, user's ruling (1600 cap, eyebrow). Same session: `TiltCard` layer hints fixed the stair-stepped tilt edge on Home (user confirmed) |
| 2026-08-26 | **`SectionSplitMediaClip` came back 🟢 (0.76.3), executed** — `HomeFoundry` on `SectionSplit` with `mediaClip={false}`; the tilt keeps its corners |
| 2026-08-26 | **`SectionTextLabelVoice` came back 🟢 (0.76.2 / 0.57.1), bumped; `SectionSplitMediaClip` filed.** The restored Home foundry tilt renders with cut corners — the DS split frame clips a rotating node. `TiltCard` given its own radius meanwhile |
| 2026-08-26 | **`SectionTextLabelVoice` filed to kol-ds-ui; Home foundry tilt restored.** The 08-15 swap of HomeFoundry onto the DS split dropped `TiltCard` (the hover tilt) for a flat `<img>` + frame zoom — never approved, flagged only as an eyeball owed. `TiltCard` is back in the media slot, `mediaHover` off. Studio hero on `80`, not `full` (user) |
| 2026-08-26 | **`SectionHeroCarouselSeams` came back 🟢 (component 0.76.1) and was adopted** — `/foundry` is on `SectionHero media=[…]`; no `FeaturedCarousel` import left in web. Every hero on the site is now one component |
| 2026-08-26 | **`SectionHeroRound2` came back 🟢 and was adopted; `SectionHeroCarouselSeams` filed.** component 0.76.0 · theme 0.57.0. One hero across the site now: Studio (`full`, dark wash, mood-05 still — the HLS video dropped, it spun the user's fans), Stack (`justify="end"` + veil + Featured card in `foot`; `StackHero` retired), typeface pages (`60`), licensing (text-only hero + `SectionFaq`). `FoundryCTA` gone from three call sites for `SectionCta variant="centered"`. `/foundry` index stays on `FeaturedCarousel` until the hero's carousel branch forwards the title/CTA/nav seams |
| 2026-08-26 | **`SectionHeroRound2` filed to kol-ds-ui** — one hero component is the rule. Done here on current packages: Studio hero `screen` under the navbar (top padding gone), typeface pages `h-[60vh]` (was 560/768px), foundry index carousel `h-[80vh]`. Stack stays on its local 90vh hero until the DS hero can pin content to the foot. Same pass: bumped component ^0.75.0 · theme 0.56.0 · workshop ^0.24.1 · foundry ^0.6.2 · store ^0.2.1 · content ^0.9.2, one kol-component, builds green |
| 2026-08-26 | **`SectionSet` came back 🟢 the same hour** — component 0.71.0 · theme 0.54.0. Bumped both apps; Studio is on the set: `SectionHero` replaces the FeaturedCarousel + hand-rolled `StudioAboutCard` overlay (retired to `_tmp/2026-08-26-studio-sectionset/`), `StudioProcessCard` on `SectionSplit`, cards on `SectionCards`, connect on `SectionCta`. Copy verbatim throughout. Same session: takeover menu heading split into its three lines (approved), open/close tweens trimmed |
| 2026-08-26 | **`FrameworkComponentPeer` + `FeaturedCarouselFullWidth` came back 🟢 within the hour** — framework 0.25.0 · component 0.70.0 (peers everywhere; `fullWidth` slides fill the stage, off-stage videos pause). Bumped both apps, deleted the workspace override, `pnpm why kol-component` → one version on peers alone. Studio hero eyeball at 1440 is the user's |
| 2026-08-26 | **`FeaturedCarouselFullWidth` filed to kol-ds-ui.** The studio hero's two-videos-at-once is the DS slide clamp (`kol-framework.css:502`, 72vw/900px) surviving `fullWidth`. No stopgap here — Studio's hero is a `SectionSet` adopter on return |
| 2026-08-26 | **`MediaLibraryReconcile` remainder executed + `FrameworkComponentPeer` filed.** component ^0.69.1 + theme 0.53.0 in both apps. `pnpm why kol-component` → 2 versions: kol-framework 0.24.0 nests 0.68.1 (`dependencies` caret — the class NestedDsDependencies left for the framework's next publish). Override added in `pnpm-workspace.yaml` (pnpm-10 home, commented), one copy verified in both apps, ticket filed with the override as its delete-on-return remainder. `/library` eyeball against media.kolkrabbi.io is the user's |
| 2026-08-26 | **`SectionSet` filed to kol-ds-ui (a SET).** Studio review found the hero's text card is the one non-DS piece on the page and that every DS section organism hand-types its text and media. Filed the composition layer the 08-15 `SectionSplit` ticket never got: `SectionText` molecule + `ContentMedia` reused, hero / split / cards / cta / faq rebuilt on them under the `Section` prefix, `align` instead of a centred sibling. Same hour: `MediaLibraryReconcile` came back 🟢 (component 0.69.0 · theme 0.53.0) — remainder is the bump |
| 2026-08-26 | **`MediaLibraryReconcile` filed to kol-ds-ui.** Brand `/library` (DS `MediaLibrary` page variant) and media.kolkrabbi.io (kol-r2b2 `FileList`) render one bucket two ways — Finder disclose-in-place, foot path bar, ~155px tiles, no sort direction / Flat / paging here vs breadcrumb + stats on top, click-to-enter rows, 260px tiles, direction, Flat, paging there. The DS copy is the weaker; the fix is the organism, not this consumer |
| 2026-08-26 | **`NestedDsDependencies` came back 🟢 the same day** — the five app packages republished with the DS tier as peers (workshop 0.23.0 · chess 0.7.0 · content 0.9.0 · foundry 0.6.0 · store 0.2.0). Bumped here with framework 0.24.0 + theme 0.52.1 riding along, the root override block deleted, and the ticket's bar met on peers alone: one kol-component in the tree. Same session: the takeover menu's scroll lock never released on close (`useGSAP` with `dependencies` reverts only on unmount) — moved to `useEffect`, reproduced on production, verified released locally; prints overlay defaults to A1 limited |
| 2026-08-26 | **`brand-redeploy-frees-media-hostname` closed — the queue is at ZERO again.** The deploy had landed 08-25; today's check on production closed the DoD (bundle free of `media.`, `/library` on `r2.`), and the receipt went back to kol-r2b2. Same day, fonts: production served `/fonts/Right-Grotesk/` while theme ≥0.41.0 asks `/fonts/right-grotesk/` — the 08-14 rename never reached git (macOS ignores case); a two-step `git mv` fixed both sites |
| 2026-08-26 | **Bump wave + the five mobile receipts squared 🟢 — and a packaging defect found under them.** component ^0.68.1 · framework ^0.23.0 · theme 0.51.0 (MobileTouchFloor came back ruled AND built while this session sat in `/ag-init`). Remainders executed: stopgap deleted, six dashboard rows stack below `md`, AssetTable buttons + Reference/Gallery links carry a 24px hit box, glyphs untouched. The verify pass showed the bump reaching NOTHING inside kol-workshop: `ShellLayout` resolved kol-framework **0.20.1**, the docs reader kol-component **0.39.0** — each `@kolkrabbi/*` app package pins the others as `dependencies` with 0.x carets, so pnpm nests its own stale copy (chess 0.38.0 · content 0.63.0 · foundry 0.52.0 · store **0.8.0**; only kol-dashboards declares peers). Root `pnpm.overrides` now forces one copy of component/framework/theme/icons — after which the active tab scrolls into view, the CodeBlock wraps with stamped lines, and prints/chess/foundry render clean at 393. Filed to kol-ds-ui as `NestedDsDependencies` with a 📌 to delete the override block once the packages declare peers. Re-walk finds fixed same pass: Gallery header under the package hamburger (`pl-16 md:pl-5`), four `/assets` ledes overflowing on path tokens (`break-words`). Not touched: brand `/components` demo page is 569 wide at 393 |
| 2026-08-26 | **Four of the five mobile briefs came back 🟢** — kol-framework 0.23.0 (SideNav's box in its rule; ShellHeader scrolls the active tab into view), kol-component 0.68.1 + kol-theme 0.50.2 (CodeBlock wraps; copy-control lane on chipless blocks). `TableMobileScroll` closed with nothing built: the wrapper has always scrolled, headless could not drag it. Remainders here: bump all three, delete the sidenav stopgap, stack the `DashboardComponents.jsx` two-up rows (that half was ours). `MobileTouchFloor` ruled the same day → **theme 0.51.0**: no type floor, 24px hit floor (bare ToggleSwitch extent, Slider input 24px) — bump, and give our own 16px table buttons and 12px links the same hit box |
| 2026-08-25 | **Five mobile briefs filed to kol-ds-ui** off the mobile audit (`plans/2026-08-25-mobile-audit-web-brand.md`): `SideNavMobilePosition` (every brand page opened one viewport down — a className utility beating the package's media rule; one-rule stopgap kept here until it ships) · `TableMobileScroll` · `CodeBlockMobileOverflow` · `WorkshopShellMobile` · `MobileTouchFloor` (🔴 ruling request). Local fixes shipped the same pass: takeover menu stacks below `md`, licensing `md:min-w-[720px]`, typeface style row stacks, specimen header wraps, hero title floor 3rem |
| 2026-08-15 | **`brand-redeploy-frees-media-hostname` filed from kol-r2b2.** That repo renamed the three stores (`r2.` / `b2.` / `b2v.`) so the admin app could take the `media.` name; `media.` is still attached to the R2 bucket and cannot be detached while brand's deployed bundle resolves `/library` images through it. Source here is already correct — `apps/brand` on media-client `^0.1.2`, `dist/` rebuilt and verified to reference only `r2.` and `admin.`. The ask is a deploy, nothing more. A CLI deploy from the filing session was deliberately rejected: `kol-brand` is Git-connected with dashboard build settings and the checkout was unlinked |
| 2026-08-15 | **`ArticleCardSizeSpec` closed same-day — the queue is at ZERO.** The user ruled all of it in-session: three presets (`readmore` dropped — a read-more band is a context rendering `mini` + `label`, not a size), the geometry table (120×120 confirmed as a ruling, clamps 2/3/2, breakpoint swaps consumer-side, seams replace-only), scope WIDE (THE listing card for any content type), name `ListingCard` (aliases until next major). Spec filed to kol-ds-ui as `ListingCardSpec` with receipt; entry graduated to `.kol/llm-context/lobby-archive/`. Same pass: the two stale 📌 rows (RecordManager · SideNavGrabResize) squared to 🟢 `none` — both receipts had recorded their remainders closed/executed since 08-09/08-14; the ledger rows had never been synced |
| 2026-08-15 | **Sections squared to the standard.** This ledger carried `States` · `Queue` · `Filed elsewhere` and nothing else — no `Closed`, no `Archived`, no `History` — so a closing ticket had nowhere to be recorded and every event here went unwritten. The gap was invisible because nothing checked: `bin/lobby --lint` now enforces the six sections across every registered ledger. A lobby is one shared system; a per-repo section list is drift, not dialect |
