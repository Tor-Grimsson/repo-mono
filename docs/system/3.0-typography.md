# Typography Reference

**Version:** 1.1
**Last Updated:** 2025-10-16
**Status:** Stable

> This chapter is part of the kolkrabbi design system. Start with [`1.0-design-system.md`](./1.0-design-system.md) for the high-level overview.

## Quick Style Guide

| Class | Font / Weight | Size (Mobile → Desktop) | Usage Notes |
|-------|---------------|--------------------------|-------------|
| `.kol-heading-display` | Right Grotesk Tight · 500 | `48px → 96px` (clamp) | Hero headlines, marquee statements (uppercase) |
| `.kol-heading-section` | Right Grotesk Tight · 500 | `40px → 64px` | Section intros on marketing pages (uppercase) |
| `.kol-heading-section-small` | Right Grotesk Tight · 500 | `32px → 48px` (clamp) | Dense layouts needing compact section headers |
| `.kol-heading-subsection` | Right Grotesk Tall · 500 | `48px` fixed | Tall, editorial subsections (uppercase) |
| `.kol-heading-xl` | Right Grotesk Narrow · 500 | `40px → 64px` | Primary content headings (1:1 with `.kol-h1`) |
| `.kol-heading-lg` | Right Grotesk Narrow · 500 | `32px → 48px` | Secondary headlines (1:1 with `.kol-h2`) |
| `.kol-heading-md` | Right Grotesk Narrow · 500 | `28px → 40px` | Tertiary headings (1:1 with `.kol-h3`) |
| `.kol-heading-sm` | Right Grotesk Tight · 500 | `20px → 32px` | Label-style headings, cards (uppercase) |
| `.kol-text` / `.kol-body` | Inter Tight · 400 | `14px → 18px` | Standard body copy; `.kol-body` is the legacy alias |
| `.kol-body-lg` | Inter Tight · 400 | `18px` | Feature paragraphs, hero copy |
| `.kol-text-sm` / `.kol-body-sm` | Inter Tight · 400 | `12px → 16px` | Captions, dense controls |
| `.kol-mono-text` / `.kol-mono-body` | Right Grotesk Mono · 400 | `14px → 18px` | Code, data tables, technical callouts |
| `.kol-mono-text-label` | Right Grotesk Mono · 400 | `12px → 16px` | Uppercase mono labels with 0.2em tracking for collapsible headings |
| `.kol-mono` | Right Grotesk Mono · 400 | `11px → 14px` | Compact mono details (chips, inline code) |
| `.kol-mono-xs` | Right Grotesk Mono · 400 | `11px → 14px` | Metadata inside UI controls |
| `.kol-mono-xxs` | Right Grotesk Mono · 400 | `8px → 12px` | Icon labels, ultra-dense glyph grids |
| `.kol-label` | Right Grotesk Mono · 400 | `14px → 24px` | Uppercase labels, pill chips, section tags |
| `.kol-label-compact` | Right Grotesk Compact · 500 | `16px → 24px` | Compact uppercase labels (pairs with SectionLabel atom) |

> Every class consumes `color: var(--component-fg)` (currently aliased to `--foreground`) so text automatically adapts inside `.surface-inverse` contexts when paired with `.text-auto`.

## Recommended Combinations
- **Mono Status Label** — `kol-mono-text-label`  
  Use for section toggles, collapsible headers, and miniature status badges. The class bakes in mono font, uppercase, and 0.2em tracking.
- **Card Caption** — `kol-mono-text text-xs opacity-70`  
  Ideal for metadata rows, table captions, or utility explanations inside previews.
- **Hero Subtitle** — `kol-label kol-label-compact` stacked with `.text-auto`  
  Combines the compact uppercase label with adaptive color for hero support copy.

## Core Families
- **Right Grotesk Tight** – Display/section typography, uppercase voice.
- **Right Grotesk Narrow** – Content headings with high readability.
- **Right Grotesk Tall** – Editorial, ornamental subsections.
- **Inter Tight** – Body copy and long-form reading.
- **Right Grotesk Mono** – Tag controls, metadata, code snippets.

Font family tokens are defined in `packages/ui/theme.css` and consumed via shared atoms. Importing `@kol/ui/theme.css` in an app guarantees the fonts are available.

## Heading Hierarchy
- **Display & Section**: `.kol-heading-display`, `.kol-heading-section`, `.kol-heading-section-small`, `.kol-heading-subsection`.
- **Content Headings**: `.kol-heading-xl`, `.kol-heading-lg`, `.kol-heading-md`, `.kol-heading-sm`.
- **Legacy Aliases**: `.kol-h1` → `.kol-heading-xl`, `.kol-h2` → `.kol-heading-lg`, `.kol-h3` → `.kol-heading-md`, `.kol-h4` → `.kol-heading-sm`. These remain until the 2025-11-01 cleanup.

## Body & Utility Text
- `.kol-text` / `.kol-body` for default paragraphs.
- `.kol-text-sm` / `.kol-body-sm` for captions and dense UI.
- `.kol-body-lg` for marketing blurbs or highlighted copy.
- `.kol-mono-text` / `.kol-mono-body` for technical descriptions or code, keeping line-height at 125%.
- `.kol-label` and `.kol-label-compact` power SectionLabel/SectionHeader atoms—use them instead of ad-hoc uppercase classes.

## Implementation Tips
1. **Use atoms first**: Components like `<SectionHeader>` and `<Tag>` already map the correct classes. Reach for raw classes only when building new primitives.
2. **Pair with `.text-auto`**: Ensures automatic contrast adjustments across light/dark and inverse surfaces.
3. **Respect responsive steps**: Avoid hardcoding font sizes—let the media queries baked into the class scaling handle device differences.
4. **Clean up legacy classes**: When touching older code, swap `.kol-h1` → `.kol-heading-xl` (and so on) to converge on the semantic naming.

---

## Remaining Issues & Next Steps

- Track the scheduled 2025-11-01 cleanup that removes `.kol-h*` aliases and update this doc once the migration completes.
- Audit typography atoms to confirm they continue to pull `--component-fg` aliases after Phase 6 removes deprecated tokens.
- Document recommended pairings for the new color state utilities once component updates land.

**Version History:**
- v1.1 (2025-10-16): Renamed to numbered format, clarified foreground aliasing, logged outstanding follow-ups
- v1.0 (2024-10-04): Initial reference draft
