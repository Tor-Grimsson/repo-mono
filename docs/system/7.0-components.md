# React Components (v7.0)

**Status:** Active
**Last Updated:** 2025-01-27
**Version:** 7.0 - Component Architecture & Locations

Documentation of reusable React components across the design system, organized by atomic design principles.

---

## Component Architecture

The component system follows atomic design principles:

- **Atoms**: Basic building blocks (buttons, labels, inputs)
- **Molecules**: Simple combinations of atoms (cards, feature items)
- **Organisms**: Complex UI sections (card grids, navigation)

### Import Patterns

Components are exported from two locations:

1. **Shared UI Package**: `/packages/ui/src/` - Core design system components
2. **App-Specific**: `/apps/web/src/components/styleguide/` - App-specific implementations

```jsx
// Import from shared UI package
import { Button, SectionLabel, Icon } from '@kol/ui'

// Import app-specific components
import CardFeatures from '../../styleguide/organisms/CardFeatures'
import CardFeatureItem from '../../styleguide/molecules/CardFeatureItem'
```

---

### Component Structure Pattern (Preferred)

**When creating components with variants, use explicit mapping objects instead of string interpolation.**

This pattern provides better clarity, type safety, and makes all available variants immediately visible.

#### ✅ Preferred Structure

```jsx
/**
 * Example Component with Variants
 */
const ComponentName = ({ children, variant = 'default', className = '' }) => {
  const variantClasses = {
    default: 'component-default',
    secondary: 'component-secondary',
    outline: 'component-outline'
  }

  return (
    <div className={`${variantClasses[variant]} ${className}`.trim()}>
      {children}
    </div>
  )
}
```

**Benefits:**
- ✅ All variants visible at a glance
- ✅ Explicit mapping (no string interpolation magic)
- ✅ Easier to add variant-specific logic
- ✅ Safer - typos in variant prop won't silently create invalid classes
- ✅ Better developer experience when reading code

#### ❌ Avoid String Interpolation

```jsx
// Don't do this - less clear what variants exist
const ComponentName = ({ children, variant = 'default', className = '' }) => {
  const variantClass = `component-${variant}`

  return (
    <div className={`${variantClass} ${className}`.trim()}>
      {children}
    </div>
  )
}
```

**Real Example:** See `Pill.jsx` (`packages/ui/src/atoms/Pill.jsx`) for the canonical implementation of this pattern.

---

## Atoms

### Button

**Location**: `/packages/ui/src/atoms/Button.jsx`

**Import**: `import { Button } from '@kol/ui'` or `import Button from '@kol/ui/atoms/Button'`

**Description**: Primary interactive element with multiple variants and icon support

**Props**:
- `variant`: `'primary' | 'secondary' | 'outline' | 'ghost'` (default: `'primary'`)
- `size`: `'sm' | 'md' | 'lg'` (default: `'md'`)
- `uppercase`: `boolean` - Text transform uppercase (default: `false`)
- `iconLeft`: Icon name to display on the left
- `iconRight`: Icon name to display on the right
- `iconLeftHover`: Icon to show on hover (left position)
- `iconRightHover`: Icon to show on hover (right position)
- `iconOnly`: Icon name for icon-only button
- `iconSize`: Size in pixels (default: `16`)
- `onClick`: Click handler function
- `className`: Additional CSS classes
- `children`: Button content

**Available Icons**:
```
arrow-downright    arrow-up       component     foundation
logo               styleguide     theme-toggle
```

**Typography**: Size classes control all typography (font-family, font-weight, font-size, line-height, letter-spacing)
- All sizes use Right Grotesk Mono (medium weight 500) with 100% line-height and 3% letter-spacing
- `sm`: 14px font-size
- `md`: 16px font-size (default)
- `lg`: 18px font-size

---

#### Button Variants

| Variant | Visual Style | Use Case | Example |
|---------|-------------|----------|---------|
| `primary` | Filled inverse background | Primary CTA, most important action | "Get Started", "Submit" |
| `secondary` | Split inverse background | Secondary action | "Learn More", "View Details" |
| `outline` | Transparent with border | Tertiary action, less emphasis | "Cancel", "Skip" |
| `ghost` | Transparent, no border | Minimal action, inline links | "Dismiss", "Close" |

---

#### Basic Usage Examples

```jsx
{/* Primary button - default variant */}
<Button>
  Get Started
</Button>

{/* Secondary button */}
<Button variant="secondary">
  Learn More
</Button>

{/* Outline button */}
<Button variant="outline">
  Cancel
</Button>

{/* Ghost button */}
<Button variant="ghost">
  Skip
</Button>
```

---

#### Button Sizes

```jsx
{/* Small button */}
<Button size="sm">
  Small Button
</Button>

{/* Medium button - default */}
<Button size="md">
  Medium Button
</Button>

{/* Large button */}
<Button size="lg">
  Large Button
</Button>
```

---

#### Text Transform

```jsx
{/* Uppercase - default */}
<Button>
  Get Started
</Button>

{/* Normal case */}
<Button uppercase={false}>
  Get started
</Button>

{/* Mixed with other props */}
<Button variant="secondary" size="sm" uppercase={false}>
  Learn more
</Button>
```

---

#### Buttons with Icons

```jsx
{/* Icon on the left */}
<Button iconLeft="foundation">
  To Foundry
</Button>

{/* Icon on the right */}
<Button iconRight="arrow-downright">
  View More
</Button>

{/* Both icons */}
<Button iconLeft="component" iconRight="arrow-up">
  Components
</Button>

{/* Custom icon size */}
<Button iconLeft="foundation" iconSize={20}>
  Larger Icon
</Button>

{/* Icon-only button */}
<Button iconOnly="theme-toggle" aria-label="Toggle theme" />
```

---

#### Hover Icon Swap

```jsx
{/* Icon changes on hover */}
<Button iconLeft="foundation" iconLeftHover="arrow-downright">
  Hover to see arrow
</Button>

{/* Right icon swap */}
<Button iconRight="component" iconRightHover="arrow-up">
  Hover effect
</Button>
```

---

#### Real-World Examples

```jsx
{/* Navigation button */}
<Button variant="primary" iconLeft="foundation">
  To Foundry
</Button>

{/* Learn more action */}
<Button variant="secondary" iconRight="arrow-downright">
  Explore Features
</Button>

{/* Cancel/dismiss action */}
<Button variant="outline">
  Cancel
</Button>

{/* Theme toggle */}
<Button
  variant="ghost"
  iconOnly="theme-toggle"
  onClick={toggleTheme}
  aria-label="Toggle dark mode"
/>

{/* Call to action with arrow */}
<Button variant="primary" iconRight="arrow-up" size="lg">
  Start Your Project
</Button>
```

---

#### Button Click Handlers

```jsx
{/* Simple click handler */}
<Button onClick={() => console.log('Clicked!')}>
  Click Me
</Button>

{/* Navigation */}
<Button onClick={() => navigate('/foundry')}>
  Go to Foundry
</Button>

{/* Form submission */}
<Button type="submit" onClick={handleSubmit}>
  Submit Form
</Button>
```

---

#### Non-Interactive Buttons

Use `pointer-events-none` to create button-styled elements that are non-interactive (no hover states, no clicks):

```jsx
{/* Non-interactive badge using button styling */}
<Button variant="primary" iconRight="foundation" className="pointer-events-none">
  Málrómur Aa
</Button>

{/* Styled label that looks like a button */}
<Button variant="secondary" className="pointer-events-none">
  Status: Active
</Button>
```

**When to use:**
- Displaying information in button-style containers
- Non-clickable badges or labels that match button aesthetics
- Maintaining visual consistency when mixing interactive and non-interactive elements

---

### SectionLabel

**Location**: `/packages/ui/src/atoms/SectionLabel.jsx`

**Import**: `import { SectionLabel } from '@kol/ui'` or `import SectionLabel from '@kol/ui/atoms/SectionLabel'`

**Description**: Section marker with animated arrow icon, used for headings and category labels

**Props**:
- `text`: Label text (string, required)
- `size`: `'sm' | 'md' | 'lg'` (default: `'md'`)
- `className`: Additional CSS classes (optional)

**Typography Classes by Size**:
- `sm` - 16px icon, uses `.kol-label-compact-md`
- `md` - 24px icon, uses `.kol-label-compact-lg` (default)
- `lg` - 32px icon, uses `.kol-heading-md`

**Features**:
- Animated arrow icon on hover (arrow-downright)
- Icon swap animation using `.icon-swap-container`
- Uppercase text with letter-spacing (sm/md only)

---

#### SectionLabel Size Variants

| Size | Icon Size | Typography Class | Use Case |
|------|-----------|-----------------|----------|
| `sm` | 16px | `.kol-label-compact-md` | Compact sections, cards, tight spaces |
| `md` | 24px | `.kol-label-compact-lg` | Standard section headers (default) |
| `lg` | 32px | `.kol-heading-md` | Large section headers, page titles |

---

#### Basic Usage Examples

```jsx
{/* Small variant - compact sections */}
<SectionLabel text="Type Design" size="sm" />

{/* Medium variant - default */}
<SectionLabel text="Selected Work" />
<SectionLabel text="Selected Work" size="md" />

{/* Large variant - prominent headers */}
<SectionLabel text="Featured Projects" size="lg" />
```

---

#### Real-World Examples

```jsx
{/* Card section header */}
<div className="card-wrapper">
  <SectionLabel text="Type Design" size="sm" />
  <h2 className="kol-heading-lg">
    Develop a sleek & timeless brand identity
  </h2>
  <p className="kol-mono text-auto">
    Visual language, defined by foundational principles...
  </p>
</div>

{/* Main section header */}
<section>
  <SectionLabel text="Selected Work" size="md" />
  <div className="grid">
    {/* section content */}
  </div>
</section>

{/* Page-level header */}
<header>
  <SectionLabel text="Case Studies" size="lg" />
  <h1>Our Latest Projects</h1>
</header>
```

---

#### With Additional Styling

```jsx
{/* Custom spacing */}
<SectionLabel text="Featured" size="sm" className="mb-6" />

{/* Custom text color */}
<SectionLabel text="Highlights" size="md" className="text-accent-primary" />

{/* Combined with flexbox */}
<div className="flex items-center justify-between">
  <SectionLabel text="Recent Posts" size="lg" />
  <Button variant="ghost">View All</Button>
</div>
```

---

#### Animation Behavior

The SectionLabel includes a built-in hover animation:
- Arrow icon rotates and swaps on hover
- Smooth transition using `.icon-swap-container`
- No additional props needed - animation is automatic

```jsx
{/* Animation works automatically on all sizes */}
<SectionLabel text="Hover Me" size="sm" />
<SectionLabel text="Hover Me" size="md" />
<SectionLabel text="Hover Me" size="lg" />
```

**Typography**: Size determines typography class used (see table above)

---

### ControlButton

**Location**: `/packages/ui/src/atoms/ControlButton.jsx`

**Import**: `import { ControlButton } from '@kol/ui'` or `import ControlButton from '@kol/ui/atoms/ControlButton'`

**Description**: Utility button with fixed padding for toggles and contextual controls. Used for control panels, settings, and utility actions.

**Props**:
- `children`: Button content (string, required)
- `onClick`: Click handler function
- `className`: Additional CSS classes (optional)
- `style`: Inline styles (optional)
- `type`: Button type attribute (default: `'button'`)
- `disabled`: Disabled state (boolean)

**Styling**:
- Padding: Fixed `8×16px` (8px vertical, 16px horizontal) across all breakpoints
- Border radius: `4px` (less rounded than standard buttons)
- Typography: Uses `.kol-mono-text` class
- Text transform: Uppercase
- Single grey variant that adapts to surface context

**Visual Characteristics**:
- Less rounded corners (4px) than standard buttons (varies by size)
- Compact, utility-focused appearance
- Inverted colors: dark on light / light on dark
- Fixed padding regardless of screen size

**Context-Aware**: Automatically adapts colors based on surface context:
- Default surface: Dark button on light background
- Inverse surface: Light button on dark background

---

#### Basic Usage

```jsx
{/* Simple control button */}
<ControlButton onClick={handleClick}>
  Show Controls
</ControlButton>

{/* Toggle button */}
<ControlButton onClick={togglePanel}>
  Hide Panel
</ControlButton>

{/* Settings action */}
<ControlButton onClick={openSettings}>
  Settings
</ControlButton>
```

---

#### With Custom Styling

```jsx
{/* Additional classes */}
<ControlButton onClick={handleClick} className="mt-4">
  Apply Changes
</ControlButton>

{/* Custom positioning */}
<ControlButton
  onClick={handleClick}
  style={{
    position: 'absolute',
    bottom: '32px',
    right: '0'
  }}
>
  Show Controls
</ControlButton>
```

---

#### Real-World Example

```jsx
// WorkHeroSection pattern - control panel toggle
{!showControls && (
  <ControlButton
    onClick={() => setShowControls(true)}
    style={{
      position: 'absolute',
      bottom: '32px',
      right: '0',
      zIndex: 100
    }}
  >
    Show Controls
  </ControlButton>
)}

// Settings panel toggle
<ControlButton onClick={toggleSettings}>
  {settingsOpen ? 'Close' : 'Open'} Settings
</ControlButton>
```

---

#### When to Use

**Use ControlButton when:**
- Creating control panel toggles
- Building utility/settings buttons
- Need compact, fixed-size button
- Want consistent sizing across breakpoints

**Use regular Button instead when:**
- Primary page actions (CTAs)
- Navigation buttons
- Need size variants (sm/md/lg)
- Want variant options (primary/secondary/outline)

**Comparison to Button component:**
- Button: Variable padding by size, multiple variants, responsive sizing
- ControlButton: Fixed 8×16px padding, single variant, same size everywhere

---

### Pill

**Location**: `/packages/ui/src/atoms/Pill.jsx`

**Import**: `import { Pill } from '@kol/ui'` or `import Pill from '@kol/ui/atoms/Pill'`

**Description**: Small badge/tag component for labels, categories, or status indicators. Wraps existing pill classes from `packages/ui/css/components.css` for consistent styling across light/dark themes.

**Props**:
- `children`: Text content of the pill (string, required)
- `variant`: Visual variant - `'outline' | 'subtle' | 'inverse'` (default: `'outline'`)
- `size`: Size variant - `'sm' | 'md' | 'lg'` (default: `'md'`)
- `className`: Additional CSS classes for customization (optional)

**Underlying Classes**: (defined in `packages/ui/css/components.css`)
- Variant classes: `.pill-outline`, `.pill-subtle`, `.pill-inverse`
- Size classes: `.pill-sm`, `.pill-md`, `.pill-lg`

**Common Styling** (all variants):
- Padding: `4px 16px` (fixed across all sizes)
- Border radius: `9999px` (fully rounded)
- All use context-aware color tokens

**Typography**: Size classes control font-size and font-weight
- All sizes use Right Grotesk Mono (medium weight 500)
- `sm`: 10px font-size
- `md`: 12px font-size (default)
- `lg`: 14px font-size

**Context-Aware**: All variants use semantic tokens (`--kol-surface-*`, `--kol-border-default`) that adapt to surface contexts automatically.

---

#### Variant Comparison

| Variant | Background | Border | Text Color | Use Case |
|---------|-----------|--------|------------|----------|
| `outline` | `--kol-surface-primary` | `1px solid --kol-border-default` | `--kol-surface-on-primary` | Default, light background with visible border |
| `subtle` | `--kol-surface-secondary` | None | `--kol-surface-on-primary` | Subtle appearance for inverse treatments |
| `inverse` | `--kol-surface-on-primary` | None | `--kol-surface-primary` | Inverted colors for emphasis |

---

#### Basic Usage

```jsx
{/* Outline variant - default */}
<Pill>Variable Font</Pill>
<Pill variant="outline">Variable Font</Pill>

{/* Subtle variant */}
<Pill variant="subtle">New Release</Pill>

{/* Inverse variant */}
<Pill variant="inverse">Featured</Pill>
```

---

#### Size Variants

```jsx
{/* Small pill - 10px */}
<Pill size="sm">Small</Pill>

{/* Medium pill - 12px (default) */}
<Pill size="md">Medium</Pill>
<Pill>Medium</Pill>

{/* Large pill - 14px */}
<Pill size="lg">Large</Pill>

{/* Combining size and variant */}
<Pill variant="inverse" size="lg">Premium</Pill>
```

---

#### Real-World Examples

```jsx
{/* Category tag - outline */}
<Pill>Type Design</Pill>

{/* Status indicator - subtle */}
<Pill variant="subtle">In Progress</Pill>

{/* Font classification - outline */}
<Pill variant="outline">Variable Font</Pill>

{/* Featured badge - inverse */}
<Pill variant="inverse">Premium</Pill>

{/* License info - outline */}
<Pill>Free License</Pill>
```

---

#### With Custom Styling

```jsx
{/* Additional spacing */}
<Pill className="mb-4">Featured</Pill>

{/* In a flex container */}
<div className="flex gap-2">
  <Pill>Design</Pill>
  <Pill variant="subtle">Typography</Pill>
  <Pill variant="inverse">Branding</Pill>
</div>
```

---

#### Variant Selection Guide

**Use `outline` when:**
- Default pill appearance needed
- Want visible border for definition
- Standard surface context

**Use `subtle` when:**
- On inverse surface contexts
- Want less visual weight
- Grouping with other subtle elements

**Use `inverse` when:**
- Need high contrast/emphasis
- Call attention to special status
- On light backgrounds for maximum visibility

All pills use fixed `4×16px` padding and mono typography for consistency.

---

### Icon

**Location**: `/packages/ui/src/atoms/Icon.jsx`

**Import**: `import { Icon } from '@kol/ui'` or `import Icon from '@kol/ui/atoms/Icon'`

**Description**: SVG icon component with size variants

**Props**:
- `name`: Icon identifier (string)
- `size`: Icon size in pixels (number, default: 24)
- `className`: Additional CSS classes (optional)

**Usage**:
```jsx
<Icon name="arrow-downright" size={16} />

<Icon name="close" size={24} className="text-auto" />
```

---

### Dropdown

**Location**: `/packages/ui/src/atoms/Dropdown.jsx`

**Import**: `import { Dropdown } from '@kol/ui'`

**Description**: Custom dropdown with smooth animations and consistent sizing with buttons

**Props**:
- `options`: Array of option objects `[{label: string, value: any}]` (required)
- `value`: Current selected value (required)
- `onChange`: Change handler function (required)
- `size`: Size variant - `'sm' | 'md' | 'lg'` (default: `'md'`)
- `className`: Additional CSS classes (optional)

**Typography & Sizing**: Matches button sizing system exactly
- All sizes use Right Grotesk Mono (medium weight 500) with 100% line-height and 3% letter-spacing
- `sm`: 14px font-size, responsive padding
- `md`: 16px font-size, responsive padding (default)
- `lg`: 18px font-size, responsive padding

---

#### Basic Usage

```jsx
const [selectedStyle, setSelectedStyle] = useState('roman')

const styleOptions = [
  { label: 'Roman', value: 'roman' },
  { label: 'Italic', value: 'italic' }
]

<Dropdown
  options={styleOptions}
  value={selectedStyle}
  onChange={setSelectedStyle}
/>
```

---

#### Size Variants

```jsx
{/* Small dropdown */}
<Dropdown
  options={options}
  value={value}
  onChange={handleChange}
  size="sm"
/>

{/* Medium dropdown - default */}
<Dropdown
  options={options}
  value={value}
  onChange={handleChange}
  size="md"
/>

{/* Large dropdown */}
<Dropdown
  options={options}
  value={value}
  onChange={handleChange}
  size="lg"
/>
```

---

#### Real-World Example

```jsx
// Matching dropdown size with adjacent buttons
<div className="flex gap-4 items-center">
  <Button variant="primary" iconRight="foundation" className="pointer-events-none">
    Málrómur Aa
  </Button>

  <Dropdown
    options={styleOptions}
    value={selectedStyle}
    onChange={setSelectedStyle}
  />
</div>
```

**Note**: Dropdowns default to `md` size, matching the button default, ensuring visual consistency when used together.

---

## Molecules

### ButtonGroup

**Location**: `/packages/ui/src/molecules/ButtonGroup.jsx`

**Import**: `import { ButtonGroup } from '@kol/ui'`

**Description**: Container for multiple buttons with consistent spacing and alignment. Accepts an array of button configurations with all Button component props.

**Props**:
- `buttons`: Array of button objects (required)
  - Each object can contain any Button prop from the Button component
  - `label` or `children`: Button text content
  - `variant`, `size`, `uppercase`, `iconLeft`, `iconRight`, `onClick`, etc.
- `align`: `'left' | 'center' | 'right'` - Horizontal alignment (default: `'center'`)
- `className`: Additional CSS classes for container (optional)

---

#### ✅ Correct Usage Pattern

**Always wrap ButtonGroup in a container div with flex layout for proper structure:**

```jsx
<div className="flex flex-col items-center gap-2">
  <ButtonGroup
    buttons={[
      { label: 'Download font', variant: 'primary' },
      { label: 'View Specimen', variant: 'outline' }
    ]}
    align="center"
  />
</div>
```

**Why this pattern:**
- Provides natural container for additional elements (like supplementary text)
- Consistent spacing through gap instead of padding on ButtonGroup
- More flexible for layout adjustments
- Cleaner separation of concerns (container handles spacing, ButtonGroup handles buttons)

**Add padding to container div, not ButtonGroup:**

```jsx
{/* ✅ Correct */}
<div className="flex flex-col items-center gap-2 pt-10 pb-4">
  <ButtonGroup
    buttons={[
      { label: 'Download Font', variant: 'primary' },
      { label: 'View Specimen', variant: 'outline' }
    ]}
    align="center"
  />
</div>

{/* ❌ Avoid */}
<ButtonGroup buttons={buttons} align="center" className="pt-10 pb-4" />
```

**Keep content inline in JSX, not in const declarations:**

```jsx
{/* ✅ Correct - editable content stays with the component */}
<ButtonGroup
  buttons={[
    { label: 'Download Font', variant: 'primary' },
    { label: 'View Specimen', variant: 'outline' }
  ]}
  align="center"
/>

{/* ❌ Avoid - content mixed with logic/state above return */}
const buttons = [
  { label: 'Download Font', variant: 'primary' },
  { label: 'View Specimen', variant: 'outline' }
]

return (
  <ButtonGroup buttons={buttons} align="center" />
)
```

**Why inline content:**
- Easier to find and edit UI text/content
- Clearer separation: const declarations = logic/state, JSX = content/UI
- More natural for simple, static button configurations
- Reserve pre-return area for computed values and actual state

---

#### Basic Usage

```jsx
<ButtonGroup
  buttons={[
    { label: 'View Projects', variant: 'primary', onClick: handleView },
    { label: 'Learn More', variant: 'secondary', onClick: handleLearn }
  ]}
/>
```

---

#### With All Button Props

```jsx
<ButtonGroup
  buttons={[
    {
      label: 'To Foundry',
      variant: 'primary',
      size: 'lg',
      iconLeft: 'foundation',
      uppercase: false,
      onClick: () => navigate('/foundry')
    },
    {
      label: 'View More',
      variant: 'outline',
      size: 'md',
      iconRight: 'arrow-downright',
      onClick: handleViewMore
    },
    {
      label: 'Cancel',
      variant: 'ghost',
      size: 'sm',
      onClick: handleCancel
    }
  ]}
/>
```

---

#### Alignment Options

```jsx
{/* Left aligned */}
<ButtonGroup
  align="left"
  buttons={[
    { label: 'Save', variant: 'primary' },
    { label: 'Cancel', variant: 'outline' }
  ]}
/>

{/* Center aligned - default */}
<ButtonGroup
  align="center"
  buttons={[
    { label: 'Get Started', variant: 'primary' },
    { label: 'Learn More', variant: 'secondary' }
  ]}
/>

{/* Right aligned */}
<ButtonGroup
  align="right"
  buttons={[
    { label: 'Next', variant: 'primary', iconRight: 'arrow-up' }
  ]}
/>
```

---

#### With Custom Spacing

```jsx
{/* Add top/bottom padding */}
<ButtonGroup
  className="pt-10 pb-4"
  buttons={[
    { label: 'Submit', variant: 'primary' },
    { label: 'Reset', variant: 'outline' }
  ]}
/>

{/* Custom gap */}
<ButtonGroup
  className="gap-6"
  buttons={[
    { label: 'Option A', variant: 'primary' },
    { label: 'Option B', variant: 'secondary' },
    { label: 'Option C', variant: 'outline' }
  ]}
/>
```

---

#### Real-World Examples

```jsx
// FoundryHero pattern - with supplementary text
<div className="flex flex-col items-center gap-2">
  <ButtonGroup
    buttons={[
      { label: 'Download font', variant: 'primary' },
      { label: 'View Specimen', variant: 'outline' }
    ]}
    align="center"
  />

  <p className="kol-mono-xs text-auto opacity-64 pt-4">
    Free for personal and commercial use
  </p>
</div>

// FoundryTypefaceDetails pattern - with spacing
<div className="flex flex-col items-center gap-2 pt-10 pb-4">
  <ButtonGroup
    buttons={[
      { label: 'Download Font', variant: 'primary' },
      { label: 'View Specimen', variant: 'outline' }
    ]}
    align="center"
  />
</div>

// In a card component
const buttons = [
  {
    label: 'View Case Study',
    variant: 'primary',
    size: 'md',
    iconRight: 'arrow-downright',
    onClick: () => navigate(`/work/${project.slug}`)
  },
  {
    label: 'See More Projects',
    variant: 'secondary',
    size: 'md',
    onClick: () => navigate('/work')
  }
]

return (
  <div className="card-wrapper">
    <h2>Featured Work</h2>
    <p>Check out our latest projects...</p>

    <div className="flex flex-col items-center gap-2 pt-10">
      <ButtonGroup buttons={buttons} align="center" />
    </div>
  </div>
)
```

---

### CardFeatureItem

**Location**: `/apps/web/src/components/styleguide/molecules/CardFeatureItem.jsx`

**Import**: `import CardFeatureItem from '../molecules/CardFeatureItem'` (relative)

**Description**: Individual feature card displaying title, icon, visual, and description. Fixed height on mobile (304px), flexible height on desktop. Features a small icon in the header and a large visual/icon in the center.

**Props**:
- `title`: Feature title (string, required)
- `icon`: Icon name from available icons (string, optional) - Shows small in header (16px) and large in center (96px) if no visual provided
- `visual`: Custom visual element to replace center icon (JSX, optional)
- `description`: Feature description (string, required)
- `backgroundColor`: Tailwind background class (string, default: `'bg-surface-on-inverse'`)

**Available Icons**:
```
arrow-downright    arrow-up       component     foundation
logo               styleguide     theme-toggle
```

**Typography**:
- Title: `.kol-helper-md` (16px)
- Description: `.kol-mono-xs` with 50% opacity

**Styling**:
- Background: `bg-surface-on-inverse`
- Border: `border border-auto`
- Padding: `p-4` mobile, `p-6` desktop
- Height: `304px` mobile, `auto` desktop (flex to fill)
- Layout: Flex column with space-between

---

#### Basic Usage

```jsx
{/* Simple card with icon only */}
<CardFeatureItem
  title="Brand Identity"
  icon="foundation"
  description="Develop a sleek and timeless brand identity with foundational design principles"
/>
```

---

#### With Custom Visual

```jsx
{/* Card with custom image instead of icon */}
<CardFeatureItem
  title="Case Study"
  icon="component"
  visual={<img src="/project-thumbnail.png" alt="Project" className="w-full h-full object-cover" />}
  description="A detailed exploration of our approach to modern web development"
/>
```

---

#### In a Grid Layout

```jsx
{/* Multiple cards in a responsive grid */}
const features = [
  {
    title: 'Type Design',
    icon: 'foundation',
    description: 'Custom typography and font selection for brand identity'
  },
  {
    title: 'Design Systems',
    icon: 'component',
    description: 'Scalable component libraries and style guidelines'
  },
  {
    title: 'Documentation',
    icon: 'styleguide',
    description: 'Comprehensive guides for design and development teams'
  }
]

<div className="grid md:grid-cols-3 gap-6">
  {features.map((feature, index) => (
    <CardFeatureItem
      key={index}
      title={feature.title}
      icon={feature.icon}
      description={feature.description}
    />
  ))}
</div>
```

---

#### With Custom Background Colors

```jsx
{/* Using surface tokens */}
<CardFeatureItem
  title="Featured Project"
  icon="foundation"
  backgroundColor="bg-surface-secondary"
  description="Highlighted project with custom background"
/>

{/* Using container tokens */}
<CardFeatureItem
  title="Premium Feature"
  icon="component"
  backgroundColor="bg-container-elevated"
  description="Elevated card for special features"
/>

{/* Using opacity backgrounds */}
<CardFeatureItem
  title="Subtle Card"
  icon="styleguide"
  backgroundColor="bg-opacity-hex-04"
  description="Card with subtle background tint"
/>
```

---

#### Real-World Example from HomeWorkFeatures

```jsx
const features = [
  {
    title: 'Case Study 1',
    icon: 'component',
    backgroundColor: 'bg-surface-secondary',
    description: 'A detailed exploration of our approach to modern web development and design systems.'
  },
  {
    title: 'Case Study 2',
    icon: 'foundation',
    backgroundColor: 'bg-container-elevated',
    description: 'Building scalable brand identities with thoughtful design principles.'
  },
  {
    title: 'Case Study 3',
    icon: 'styleguide',
    backgroundColor: 'bg-opacity-hex-08',
    description: 'Creating cohesive visual experiences across digital products.'
  },
  {
    title: 'Case Study 4',
    icon: 'arrow-up',
    description: 'Strategic brand development from concept to implementation.'
  }
]

<div className="inline-flex flex-col md:flex-row md:h-72 gap-6">
  {features.map((feature, index) => (
    <CardFeatureItem
      key={index}
      title={feature.title}
      icon={feature.icon}
      visual={feature.visual}
      backgroundColor={feature.backgroundColor}
      description={feature.description}
    />
  ))}
</div>
```

---

### DesCard

**Location**: `/apps/web/src/components/styleguide/molecules/DesCard.jsx`

**Description**: Documentation card for styleguide display

**Props**:
- `name`: Component name (string)
- `description`: Component description (string)
- `code`: Code sample or class info (string)

---

### SurfacePreviewGrid

**Location**: `/apps/web/src/components/styleguide/molecules/SurfacePreviewGrid.jsx`

**Description**: Grid layout for previewing components across different surface contexts

**Props**:
- `layout`: `'grid' | 'stacked'`
- `children`: Surface components

**Sub-component**: `SurfacePreviewGrid.Surface`

---

## Organisms

### CardFeatures

**Location**: `/apps/web/src/components/styleguide/organisms/CardFeatures.jsx`

**Import**: `import CardFeatures from '../../styleguide/organisms/CardFeatures'` (relative)

**Description**: Complete feature card section with header, description, feature grid, and action buttons

**Props**:
- `title`: Section title (string, default: 'Selected Work')
- `description`: Section description (string)
- `features`: Array of feature objects
  - `title`: Feature title
  - `icon`: Icon element (optional)
  - `visual`: Visual element (optional)
  - `description`: Feature description
- `actions`: Array of action objects
  - `label`: Button label
  - `variant`: `'primary' | 'secondary'`
  - `onClick`: Click handler function

**Usage**:
```jsx
<CardFeatures
  title="Selected Work"
  description="Develop a sleek and timeless brand identity..."
  features={[
    {
      title: 'Brand Identity',
      icon: <Icon name="star" size={16} />,
      visual: <img src="/visual-1.svg" alt="" />,
      description: 'Create memorable brand experiences'
    },
    {
      title: 'Digital Products',
      description: 'Build modern web applications'
    }
  ]}
  actions={[
    {
      label: 'View Projects',
      variant: 'primary',
      onClick: () => console.log('View clicked')
    },
    {
      label: 'Learn More',
      variant: 'secondary',
      onClick: () => console.log('Learn clicked')
    }
  ]}
/>
```

**Dependencies**:
- `Button` from `@kol/ui/atoms/Button`
- `SectionLabel` from `@kol/ui/atoms/SectionLabel`
- `CardFeatureItem` from `../molecules/CardFeatureItem`

**Typography**:
- Title: Uses `SectionLabel` component (`.kol-label-compact`)
- Description: `.kol-mono-xs`

**Layout**:
- Container height: `h-[800px]`
- Background: `bg-opacity-hex-02`
- Border: `outline outline-1 outline-opacity-hex-08`
- Features grid: `h-72` with `gap-6`
- Responsive padding on action buttons: `px-96`

---

## Component Mapping

### Button Variants → Design Tokens

| Variant | Background | Border | Use Case |
|---------|-----------|--------|----------|
| `primary` | Filled inverse | Inverse border | Primary CTA |
| `secondary` | Split inverse | Standard border | Secondary action |
| `outline` | Transparent | Standard border | Tertiary action |
| `ghost` | Transparent | None | Minimal action |

### Action Prop Mapping in CardFeatures

When passing actions to CardFeatures:
- `action.variant: 'primary'` → Button variant: `secondary`
- `action.variant: 'secondary'` → Button variant: `outline`

This mapping prioritizes visual hierarchy (secondary buttons are most prominent after primary).

---

## File Structure

```
packages/ui/
└── src/
    └── atoms/
        ├── Button.jsx          # Primary interactive element
        ├── SectionLabel.jsx    # Section headers with arrow
        └── Icon.jsx            # SVG icon system

apps/web/
└── src/
    └── components/
        └── styleguide/
            ├── molecules/
            │   ├── CardFeatureItem.jsx      # Feature card item
            │   ├── DesCard.jsx              # Documentation card
            │   └── SurfacePreviewGrid.jsx   # Surface preview
            └── organisms/
                └── CardFeatures.jsx         # Complete feature section
```

---

## Typography Classes Used

All components use typography classes from the v3.0 system:

- **Labels**: `.kol-label-compact`, `.kol-label-compact-lg`
- **Helpers**: `.kol-helper-md`, `.kol-helper-s`
- **Mono**: `.kol-mono-xs`
- **Color**: `.text-auto` for automatic surface context

See [3.0-typography.md](./3.0-typography.md) for complete typography documentation.

---

## Color System Integration

All components use semantic color tokens from the v2.0 color system:

- **Surfaces**: `bg-surface-on-inverse`, `bg-surface-support-split-inverse`
- **Borders**: `outline-opacity-hex-08`, `outline-opacity-hex-inverse-08`
- **Opacity**: `bg-opacity-hex-02`, `bg-opacity-hex-04`

See [2.0-color-system.md](./2.0-color-system.md) for complete color documentation.

---

## Best Practices

### Component Composition

1. **Prefer composition over props**: Use children and sub-components for flexibility
2. **Keep atoms simple**: Single responsibility, minimal props
3. **Molecules should be reusable**: Extract patterns used 3+ times
4. **Organisms are page-specific**: Complex sections with specific layouts

### Import Strategy

1. **Shared components**: Always import from `@kol/ui` when available
2. **App-specific**: Use relative imports for styleguide components
3. **Avoid deep imports**: Prefer named exports from package root

### Styling Guidelines

1. **Typography**: Always use typography utility classes (`.kol-*`)
2. **Color**: Always use semantic color tokens with `.text-auto` for adaptability
3. **Spacing**: Use Tailwind spacing scale consistently
4. **Layout**: Prefer flexbox with gap over margin-based spacing

---

## Related Documentation

- [1.0-design-system.md](./1.0-design-system.md) - Overall design system architecture
- [2.0-color-system.md](./2.0-color-system.md) - Color tokens and usage
- [3.0-typography.md](./3.0-typography.md) - Typography system
- [4.0-css-architecture.md](./4.0-css-architecture.md) - CSS structure and utilities
- [5.0-styleguide.md](./5.0-styleguide.md) - Styleguide implementation

---

**Component Documentation** - See component files for additional inline documentation and implementation details.
