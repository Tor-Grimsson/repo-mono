# 4.0.2 Studio Page

**Route:** `/studio`  
**Component:** `apps/web/src/routes/Studio.jsx`  
**Status:** Active

---

## Page Goals

- Introduce Kolkrabbi’s methodology and services
- Share approach + engagement model
- Provide a direct contact path via CTA

---

## Layout Skeleton

```tsx
<main className="min-h-screen w-full overflow-x-hidden">
  <StudioHero />
  <ImageSection />
  <div className="main-wrapper">
    <StudioApproach />
    <div className="card-wrapper">
      <StudioServices />
    </div>
  </div>
  <CtaGlobal />
</main>
```

**Hero:** Hero headline, description, CTA stack (`StudioHero`).  
**Editorial Band:** Optional `ImageSection` (currently empty block reserved for future art).  
**Approach:** Narrative content describing process (`StudioApproach`).  
**Services Grid:** Card list of service pillars (`StudioServices`).  
**CTA:** Shared `CtaGlobal`.

---

## Content Notes

- SEO metadata defined in `Studio.jsx` (title “Studio — Kolkrabbi” etc.)
- All body copy managed inline for now; future Sanity schema TBD.
- `ImageSection` intentionally left blank—treat as art drop zone.

---

## Next Steps

1. Wire `ImageSection` to a curated asset or animation.
2. Move hero/approach/services copy into Sanity once the Studio schema lands.
3. Add testimonial or partner logos for social proof if needed.
