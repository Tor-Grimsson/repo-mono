# Chess Assets (Rough Drafts)

Temporary drop zone for in-progress chess visuals. Keep raw vector pieces in `raw/` until they are polished enough for production. When a set is ready, move it into the parent directory and export through the UI package as needed.
