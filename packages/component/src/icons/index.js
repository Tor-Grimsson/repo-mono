/**
 * Icons Index
 *
 * Export Icon component
 * Usage: <Icon name="arrow-up" size={16} />
 */

// Unified Icon — re-exported from @kol/loader (the canonical icon home: mirrored
// stroke/solid set + legacy + web app-specific icons). Keeps web's import path
// (@kol/ui → @kol/component) unchanged while resolving every name from one set.
export { Icon } from '@kol/loader'
