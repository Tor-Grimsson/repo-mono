{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/share/stack/:slug", "destination": "/api/share/stack/:slug" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
